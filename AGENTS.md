# Agent 指南

## 适用范围

本文件提供所在项目的持久 Agent 指令，适用于仓库内全部文档和代码修改。测试、配置、迁移、依赖、安全和可观测性属于代码的关联内容。

规则在声明的适用范围内强制执行。条件不成立时规则不适用；需要偏离适用规则时，说明范围和风险并取得明确确认。

项目目录、命令、工具版本、接口清单、环境标识和领域判据以仓库中的权威配置、规格或可运行产出为准。事实来源按项目已确认的职责和适用范围确定权威性，不按载体类型预设统一优先级；来源冲突时核对各自职责、适用范围和生成关系，仍无法唯一确定且影响当前任务时，不推测或创建临时约定，按设计阶段的问题处理。

## 原则

- **事实一致**：文档、代码、测试和配置与已确认事实保持一致，并同步处理关联变化
- **证据先行**：判断建立在可复核的文档、代码、配置、输出和运行结果上
- **根因优先**：问题经核实后定位根因，修复从根因所在层级开始
- **方案先行**：实施前明确范围、依据、预期结果、实现方法和验证方式
- **正交内聚**：文档章节、代码元素和技术抽象各自承载清晰、内聚且不重叠的职责
- **最小充分**：产出完整覆盖目标，并采用直接服务已确认目标的最小结构和修改范围；未来需求在对应交付中增加所需抽象、配置或机制
- **结构表达**：优先通过名称、类型和结构直接表达事实与行为，相同语义使用一致表达
- **验证闭环**：每项修改都经过适用检查，并形成可复核证据
- **安全有据**：权限和安全加固与已确认的安全需求一致，可观测输出限定在复核行为所需的最小范围

## 方法

文档和代码修改依次经过设计、推演和实施。阶段深度与修改规模、风险和影响范围相称；阶段可以简短，三个阶段均须完成。后续阶段发现依据失效、范围扩大或新的未决问题时，返回设计阶段重新设计和推演。

### 设计

- 每张卡片先确定「提供」和「目标」，以提供物明确可依赖的交付契约，以目标明确当前职责、结果和后续开发关系；其余章节从已确认的「提供」和「目标」派生
- 卡片默认沿用所选技术和运行环境的标准能力；仅在保证已确认的提供物、目标、跨卡片契约或安全需求确有需要时建立项目约束，且约束具有可验证的实际目的
- 读取与目标直接相关的事实来源、配置和现有产出，区分已确认事实、现状证据、实现选择和表达选择；缺陷修复先依据证据确认问题、根因和当前状态
- 选择符合既有结构、架构和约定的最小充分方案，明确正常、异常和实际存在的边界行为

设计结果完整回答以下问题：

| 要素 | 问题 |
| --- | --- |
| 范围 | 修改什么，排除什么 |
| 依据 | 为什么修改 |
| 结果 | 修改后达到什么状态 |
| 方法 | 如何实现 |
| 验证 | 如何确认结果 |

信息不足且会影响规格、范围、授权、安全、兼容性、实现选择或验证结果时，形成具体问题并取得明确结论。

### 推演

- 检查事实、术语、结构和派生内容的一致性，推演正常、异常和实际存在的边界路径
- 推演数据、配置、接口和状态变化，以及对现有文档、模块、调用方和测试的直接影响
- 推演兼容性、安全性和可观测性影响，并确定回归范围
- 确认每项新增职责具有唯一归属，每项新增抽象具有明确用途，并优先复用既有能力
- 确认方案完整覆盖目标、关联影响具有处理方式、职责边界清晰、验证可以执行且影响交付结果的问题均已解决

设计和推演结论在实施前形成可复核记录；记录可以写入现有工作说明或项目权威规格，形式与任务规模相称。需要确认的事项取得明确结论后再实施；其他事项在记录结论后进入实施。

### 实施

- 按照经推演确认的设计修改文档、代码、测试和配置，确保每项修改都能追溯到设计目标并满足本文档要求
- 适用检查从已存在且适用于当前范围的交付规格、仓库统一命令入口、CI 配置和项目工具配置中确定；声明冲突或无法判断检查是否完整时返回设计阶段处理
- 执行适用的格式、静态、类型和测试检查
- 运行时依赖变化执行受影响系统边界的端到端测试；开发工具依赖变化执行直接影响的验证；影响无法可靠判断时按运行时影响处理
- 检查实际结果与设计预期一致，并使用实际命令、检查结果和产出位置形成验证证据

检查不适用时说明判定依据，并从本次修改的适用检查集合中排除。适用检查以本次修改结果为对象；存在未执行或未通过的适用检查时，产出说明为未完成，并写明未验证项和已知风险。全部适用检查执行并通过且适用规格规定的其他完成条件成立后，产出说明为完成。经证据确认与本次修改无关的既有失败保持当前修复范围不变，并记录基线证据、阻塞项和继续完成所需条件。

## 文档

- 项目文档使用中文；外部规范和协议的原文、代码标识和通行专有名词保持其规范形式
- 标题使用简短名词并以层级表达真实从属关系；稳定字段、关系、步骤和判据使用列表、表格、代码块或其他结构化形式
- 每项项目事实和机器语义只有一个权威维护位置，派生内容随权威事实同步更新
- 叙事先说明做什么、怎么做和边界；原因有助于正确理解做法时再补充
- 一个 Markdown 叙事段落占一个物理行，不限制行宽；列表、表格和代码块遵循自身结构
- 描述直接说明应达到的状态；需要禁止行为时，写明禁止对象和边界
- 同一概念使用同一术语，同一机器语义使用同一规范写法；路径、命令、字段和代码标识使用行内代码，代码块使用准确的语言标识
- 文档完成后，目标读者能够直接理解用途、做法、边界和判据

## 代码

### 结构

- 正式代码和测试代码遵循相同的可读性要求及项目声明的格式、静态检查和类型检查配置；自动格式化配置决定具体排版
- 每个文件和模块围绕一项内聚职责组织；共同实现同一职责的类型和函数可以组成深模块，不按长度或元素数量机械拆分
- 函数完成一个可命名动作；可以协调共同构成该动作的连续步骤，具有独立契约、错误语义、复用需求或变化原因的步骤再拆分
- 函数体内的连续逻辑步骤之间以一个空行分隔，使每段表达一个完整步骤；同一步骤内的语句保持相邻
- 文件、模块、类型、函数、变量和常量使用能够区分职责、动作和数据语义的名称；相同语义使用相同名称
- 系统边界和稳定模块边界的输入与输出使用具名类型并显式声明字段及其可选性；无契约的原始键值容器限定在解析边界内部，领域允许动态键集合时使用具名类型声明约束

### 错误

- 业务拒绝、输入错误、冲突、超时和依赖暂时不可用属于可恢复错误，显式处理并以具名类型或错误码沿调用链传播，在边界统一映射为对外错误
- 未捕获异常、panic 或进程终止只用于程序错误、已破坏的不变量和无法安全继续的启动失败
- 原始异常保持在内部错误链中；仅在可观测系统将访问限制为经项目权限配置授权的人员或服务身份且内容不泄露敏感信息时，记录必要内容；原始异常不得直接返回调用方

### 依赖

- 进程外依赖通过项目拥有的能力边界访问，抽象表达业务依赖的外部能力；接口围绕业务能力组织，不逐方法复制第三方接口
- 时间、随机数和重试策略只在影响可观察行为时由调用方注入
- 引入或升级直接依赖前，核实来源、包名、版本兼容性、维护状态、已知安全风险和许可证兼容性，并在同一次修改中同步依赖声明与锁定文件
- 运行时、编译器、包管理器、测试工具和插件版本由仓库配置声明；版本变化同步处理配置、锁文件、运行说明和验证

### 状态

- 状态、缓存、索引和跨系统副本具有唯一权威源
- 缓存、索引和跨系统副本明确同步方式、允许延迟、冲突判定及重建或对账方式；发生冲突时以权威源为准
- 跨存储、跨服务和异步写入路径声明一致性边界、重试条件与上限、去重依据和部分失败恢复方式；不能安全去重的操作不得自动重试，无法形成原子事务时明确中间状态及补偿或恢复路径

### 数据

- 由人工操作创建或修改并保存当前可变业务状态的持久化记录维护审计字段；纯系统数据、派生副本、已完整表达操作人的不可变事件，以及能够由所属记录唯一确定操作人的关系记录不重复维护

| 字段 | 规则 |
| --- | --- |
| `created_by` | 创建时的操作用户名，创建后不变 |
| `created_at` | UTC 创建时间，创建后不变 |
| `updated_by` | 创建或最近修改时的操作用户名 |
| `updated_at` | UTC 创建或最近修改时间 |

- 用户名全局唯一，并按项目统一的规范化结果比较；规范化算法由身份规格或配置唯一声明，未声明时不自行选择算法；修改或注销后的旧用户名不得重新分配
- `system` 是系统保留用户名，只用于没有人工发起者的自主系统操作；异步、延迟和跨服务操作代表人工继续执行时传递原始用户名
- 审计用户名由写入方从可信身份上下文显式提供；审计时间由持久化边界使用权威数据库时间生成，不作为外部接口或应用层输入。审计字段与业务数据在同一事务或一致性边界内写入
- 表示某地墙上时间的领域值使用独立类型和时区字段，与审计时间保持类型和语义隔离
- 持久化结构的全部变更通过可追踪迁移完成；应用启动过程不得静默修改持久化结构；迁移明确前置条件、兼容范围、失败恢复和验证方式；迁移和不可逆数据变化属于高风险操作

### 配置

- 每个可独立运行的应用或服务只有一个权威配置入口；分层加载顺序固定且可追踪，版本化结构化配置提供基线值，环境变量在合并后覆盖对应字段
- 所有环境使用相同的加载、合并和校验逻辑；配置 schema 明确字段、类型、可选性、约束、项目专用环境变量命名空间及字段映射
- 配置文件中的未知或重复字段、项目专用环境变量命名空间中的未知字段、合并后的缺失字段及解析或约束错误阻止启动；其他环境变量不参与校验
- `.env` 用于向本地进程注入环境变量，可以纳入版本控制；纳入前确认不含敏感值，版本化开发凭据必须可以公开且只能访问本地、隔离、可重建并且不含真实用户数据的资源
- 其他敏感值通过环境变量提供
- 配置来源可以记录，配置值的输出仍受敏感数据和最小上下文约束

### 表达

- 文件、类型、函数、常量和复杂逻辑的职责、契约、不变量、边界条件或设计取舍无法由名称、类型和结构完整表达时添加中文注释，说明代码无法自行表达的事实而不重复现有行为
- 以生成来源为权威维护位置的机械生成文件通过生成流程更新；项目维护的生成器和模板仍遵守项目规则
- 失败、拒绝、跨边界操作和状态转换在需要事后复核时输出日志
- 项目主动输出的日志消息使用中文；机器字段、外部协议值和错误码保持规范形式，可检索信息使用结构化字段承载
- 可观测输出只包含复核行为所需的最小上下文；访问权限通过项目权限配置授予完成复核所需的人员或服务身份

### 安全

- 权限与安全加固按照已确认的安全需求实施；应用独占的数据库、schema、存储实例或桶按数据面整体授权，已确认存在主体隔离、数据隔离或安全差异时再细分到表、key、对象前缀或 Topic
- 已确认需要区分迁移、管理等控制面与运行时数据面时，在系统边界分别配置和校验对应权限
- 敏感数据不得写入仓库、测试数据、日志、普通配置、命令输出或验证证据
- 真实开发环境不得使用生产真实用户数据；需要接近生产分布的数据时，只使用合成数据或经过批准且不可逆匿名化的数据

### 测试

项目测试只分为单元测试和端到端测试，不建立独立的集成测试或契约测试分类。

在同一进程内直接验证正式代码的测试归为单元测试；任何访问进程外依赖或通过网络调用被测目标的测试均归为端到端测试。端到端测试可以覆盖完整系统，也可以覆盖具有明确外部边界的子系统。

#### 通用

- 测试覆盖被测契约实际存在的正常、错误和边界行为，每项已声明行为具有对应验证；错误分支断言具体错误类型、错误码或可观察的安全结果
- 测试断言外部可观察行为，与被测实现的内部步骤保持独立
- 覆盖率阈值由项目配置或具体交付规格维护；执行并报告全部适用覆盖率检查

#### 单元测试

- 单元测试使用被测目标的正式代码语言编写，在同一进程内直接执行正式代码，并位于所属端的 `tests/unit/`
- 正式代码目录不包含测试代码，单元测试不访问进程外依赖
- 能够确定性运行的进程内正式实现使用真实实例；替身只隔离进程外依赖、时间、随机数和难以稳定触发的故障，不通过模拟内部调用次数或步骤验证实现细节

#### 端到端测试

- 端到端测试统一使用 `pytest` 编写、收集和执行，位于所属系统的 `tests/e2e/` 并按功能领域组织
- 测试在与生产隔离且能由版本化配置重建的真实开发环境中通过真实系统边界执行；接口和依赖本身使用网络时通过真实网络访问，并使用场景涉及的真实服务进程、数据存储、消息中间件和第三方沙箱
- 环境通过项目声明的可验证身份标识和目标地址识别；执行前完成验证，标识缺失、不匹配或指向生产环境时立即失败
- 每次运行使用唯一隔离键管理账号、数据和其他可变资源；用例自行创建和清理数据，清理失败后能够集中回收
- 对外接口包括用户界面、公开 API、CLI 命令和外部事件入口；本次修改的适用接口集合以项目维护的接口定义核对，存在运行时注册机制的接口同时核对其注册结果；内部接口只有被声明为稳定契约时才单独覆盖
- 本次修改新增、修改或经影响判定需要回归的每个对外接口至少覆盖成功、每种具有不同错误码或安全结果的已声明错误，以及适用的未认证和权限不足结果
- 只有改变对外结果的边界条件进入端到端测试；可重复投递或自动重试的路径覆盖重复执行，多步跨边界写入覆盖部分失败及恢复，可能乱序时覆盖乱序
- 性能基准、安全扫描、静态分析、构建检查和迁移检查属于专项验证，使用对应专用工具，不改写为 `pytest` 端到端用例

## Agent 操作

### 确认

- 方案能够由已确认事实和规范唯一确定且满足适用规格确认条件时，由 Agent 确认，并按适用交付流程实施
- 存在实质选择、范围扩大、高风险操作、规则例外，或者用户要求先审方案时，在实施前展示经过适用设计和规格检查的完整方案或规格，说明范围和风险并取得用户明确确认
- 高风险操作至少包括身份认证、权限、安全或隐私变更，资金、计费或结算，不可逆数据操作和数据库迁移，访问或修改生产及真实用户数据，改变既有公开接口兼容性，跨系统一致性，扩大权限或敏感信息可见范围，产生非预期费用或对外发布，无法可靠恢复的外部状态变更，以及适用规格声明的其他高风险操作
- 高风险操作在完成前对完成门禁使用的同一候选事实执行适用的独立上下文检查
- 当前授权范围由用户已确认的目标和适用规格限定，并受已声明的写入边界约束，包含保持目标范围内文档、代码、测试和配置一致所必需的直接关联修改；技术关联本身不授权新增业务能力、处理与目标无直接关系的独立交付单元、执行高风险操作或执行需要单独授权的 Git 与外部状态操作
- 根因位于当前授权范围外时，只完成诊断和影响分析，不增加绕过根因的补丁；说明所需范围和不扩展的后果，取得授权后实施
- 只影响当前操作的例外记录在结果和验证证据中；持续例外记录在受影响对象的权威规格或配置中；修改本文件需要用户明确授权

### Git

- 提交只在用户明确要求时执行，并在提交前确认当前分支符合任务要求；未要求建立或切换分支时保持当前分支
- 建立或切换分支、推送、强制推送、修改远程分支、发布评论及触发外部流程分别取得明确授权；推送会触发外部流程时同时取得对应授权
- 提交前确认拟提交差异属于当前任务且全部适用检查已经通过，每个提交聚焦一项目的
- 提交信息使用 `<type>: <中文描述>` 格式，type 取值：
  - `feat`：新增行为
  - `fix`：修正行为
  - `refactor`：代码结构
  - `test`：测试
  - `docs`：文档
  - `chore`：依赖、配置和构建
