# Agent 指南

本文件指导 AI 对文档与代码修改进行设计、推演和实施，使产出保持清晰、可靠、可验证，并与既有约定一致。

## 原则

- **事实一致**：文档、代码、测试和配置与已确认事实保持一致，并同步处理关联变化
- **证据先行**：判断建立在可复核的文档、代码、输出和运行结果上
- **根因优先**：问题经核实后定位根因，修复从根因所在层级开始
- **方案先行**：实施前明确范围、依据、预期结果、实现方法和验证方式
- **正交单一**：文档章节、代码元素和技术抽象各自承载清晰且互不重叠的职责
- **最小充分**：产出完整覆盖目标，并采用完成目标所需的最小结构和修改范围
- **结构优先**：产出优先通过名称和结构直接表达事实与行为
- **清晰表达**：使用流畅、清晰、简洁、明确的语言，优先正向说明应达到的状态；需要禁止行为时，明确写出禁止对象和边界
- **一致表达**：相同语义使用一致的名称、结构和实现方式
- **验证闭环**：每项修改都经过对应检查，并形成可复核证据
- **安全最小**：数据访问、权限和可观测内容限定在完成目标所需的最小范围

## 方法

文档和代码修改依次经过设计、推演和实施。每个阶段完成后进入下一阶段；任一后续阶段发现方案问题时，返回设计阶段重新设计和推演。

### 设计

- 读取与当前目标直接相关的事实来源、配置和现有产出
- 依据可复核证据确认问题与当前状态，并明确目标
- 修复缺陷前先核实问题并定位根因
- 区分已确认事实、现状证据、实现选择和表达选择
- 明确修改范围、纳入对象和排除对象
- 选择符合既有结构、架构和约定的最小充分方案
- 信息不足时形成具体问题，取得明确结论后继续

设计结果完整回答以下问题：

| 要素 | 问题 |
| --- | --- |
| 范围 | 修改什么，排除什么 |
| 依据 | 为什么修改 |
| 结果 | 修改后达到什么状态 |
| 方法 | 如何实现 |
| 验证 | 如何确认结果 |

### 推演

- 检查事实、术语、结构和派生内容的一致性
- 推演正常路径、异常路径和边界条件
- 推演数据、配置、接口和状态变化
- 推演对现有文档、模块、调用方和测试的直接影响
- 推演兼容性、安全性和可观测性影响，并确定回归范围
- 确认每项新增职责具有唯一归属
- 优先复用能够满足方案的既有能力，并确认每项新增抽象具有明确用途
- 确认验证方式完整覆盖预期结果和关联影响
- 发现遗漏、冲突或歧义时修订设计并重新推演

推演完成需要同时满足：

- 方案完整覆盖目标
- 关联影响具有明确处理方式
- 职责边界清晰
- 验证方式可执行
- 所有设计问题均已解决

### 实施

- 按照经推演确认的设计修改文档、代码、测试和配置
- 每项修改都能追溯到设计目标
- 文档和代码分别满足下文对应的质量要求
- 执行适用的格式检查、静态检查、类型检查和测试
- 检查实际结果与设计预期一致
- 使用实际命令、检查结果和产出位置形成验证证据

实施过程中发现设计依据失效、影响范围扩大或出现新的未决问题时，停止当前实施，返回设计阶段并重新推演。

## 文档

- 文档沿用既有语言和结构约定
- 标题使用简短名词，层级表达真实从属关系
- 每项事实只有一个维护位置，派生内容随权威事实同步更新
- 稳定字段、关系、步骤和判据使用列表、表格、代码块或其他结构化形式
- 叙事先说明做什么、怎么做和边界，原因有助于正确理解做法时再补充原因
- 一个叙事段落占一个物理行，文档不限制行宽
- 描述直接说明应达到的状态；需要禁止行为时，写明对象和边界
- 同一概念使用同一术语，同一机器语义使用同一规范写法
- 路径、命令、字段和代码标识使用行内代码，代码块使用准确的语言标识
- 完成后的文档让目标读者能够直接理解用途、做法、边界和判据

## 代码

- 正式代码和测试代码遵循相同的可读性要求，并共同遵守项目声明的格式、静态检查和类型检查配置
- 每个文件和模块围绕一项内聚职责组织
- 函数完成一个可命名动作
- 文件名和模块名表达职责，类型名表达领域对象或技术职责
- 函数名表达执行动作，变量名和常量名表达数据语义
- 同一语义使用一致名称，不同语义使用不同名称
- 代码优先通过名称、类型和结构表达行为
- 不同职责、处理阶段或逻辑分支的代码块使用空行分隔；同一连续逻辑保持紧凑，不添加无意义空行
- 文件、类、方法、成员和复杂逻辑的职责、契约、不变量、边界条件或设计取舍无法由名称、类型、声明和结构完整表达时，添加必要注释
- 注释使用中文，直接说明代码无法自行表达的事实，并与当前实现保持一致；不重复描述可由代码直接读出的行为
- 项目主动输出的日志消息使用中文；机器字段、外部协议值和原始异常信息保持其规范形式
- 日志、事件、指标和跟踪输出满足已确认的行为与数据边界
- 每项可观测输出只包含预期观察者有权查看的数据
- 可观测输出包含复核行为所需的最小上下文
- 敏感数据通过批准的安全设施存储和传递
