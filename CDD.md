# 卡片式开发

CDD（Card-Driven Development）是一种面向人和 AI 协作的软件开发规范，将规格、实现边界、验证和影响处理组织为边界明确、可实现且可验收的交付卡片。

CDD 使用卡片树定位功能归属，以固定卡片记录规格、文件和验收，通过 `depends` 表达能力依赖，并用生命周期和完成门禁推进、检查交付。

CDD 适用于以中文为主要工作语言的新项目和存量项目。项目可以使用任意技术栈；技术选择由项目外部规范和卡片声明决定。CDD 可以由 AI 使用当前环境中的读取、搜索、推演和执行能力直接实施，也可以配套自动化工具提高效率。

CDD 规定卡片交付必须成立的规格、状态、依赖、文件、验证和影响事实，以及这些事实的判断与转换条件。工作区组织、事实快照、运行时派生视图、并发控制和原子更新的物理实现由外部规范或当前工具选择，并满足本文规定的可重识别、一致性和失败恢复判据；CDD 核心要求以本文明确规定的语义为准。

本文中的规范模板和标为“示例（非规范性）”的内容用于解释规则。规范要求以正文为唯一来源；示例与正文存在差异时，Card Lint、Card Review 和完成门禁按正文执行。

## 设计哲学

### 规格

CDD 将需求、设计、开发任务和验收标准收敛为一张卡片。卡片负责：

- 定义当前交付拥有的能力和边界
- 给出足以产生一致外部行为的交付规格
- 声明允许写入的实现、共享和测试文件范围
- 提供可执行验收、覆盖率要求、修订条件和完成条件

卡片聚焦当前交付需要实现和验收的功能。每项能力由实际交付该能力的卡片维护。正常行为使用流畅、清晰、简洁的语句正向表达；拒绝、失败、保密或禁止行为属于实现契约时，明确写出触发条件、保持不变的事实和安全结果。

一张卡片既是当前能力的规格，也是一次开发任务。README 链、当前卡片、必要的直接前置卡片和适用的外部规范共同限定交付上下文。内部实现可以不同，外部行为必须与规格一致。

### 增量

项目或主题从当前交付所需的最小 README 链和卡片开始。后续能力按交付顺序增加或修订，单次编码始终围绕一个局部完整的交付单元进行。

每张转换为 `ready` 的卡片必须形成完整、可实现且可验收的规格。未来功能在进入对应交付时创建或细化，使当前卡片保持最小充分范围。

实施中发现的新事实归入拥有对应能力的卡片。保持既有契约的新事实作为内部实现选择处理；改变当前或既有契约的新事实按修订和影响验证规则处理。

### 术语

| 术语 | 定义 |
| --- | --- |
| 卡片根目录 | 卡片树的 `docs/` 目录，由 `docs/README.md` 维护 |
| 主题目录 | 卡片树中包含一个 `README.md` 和至少一个直属节点的目录 |
| 节点 | 主题目录和卡片的统称 |
| 直属节点 | 当前目录直接索引的主题目录或卡片 |
| README 链 | 从卡片根目录到当前卡片所在主题目录的有序 README 序列 |
| 直接前置 | 其提供物被当前卡片直接使用的卡片 |
| 提供物 | 卡片声明并供依赖方使用的输出契约 |
| 依赖使用 | 当前卡片对一张直接前置卡片的一个或多个提供物的精确引用 |
| 文件边界 | 当前卡片在「文件」章节声明的允许写入范围 |
| 实现文件 | 当前卡片用于实现交付的预定目录或完整文件路径 |
| 共享文件 | 允许多张卡片受控修改的完整文件路径 |
| 测试文件 | 当前卡片用于验证 AC 的预定目录或完整文件路径 |
| 文件记录 | 「实现记录」中保存的当前有效创建与修改文件完整路径事实 |
| 交付路径集 | 当前卡片交付实际涉及并需要共同检查、共同生效的规格、索引、实现、共享和测试文件完整路径集合 |
| 存量文件 | 项目开始采用 CDD 时已经存在，且不由任何卡片创建的既有文件 |
| 采用基线 | 项目开始采用 CDD 时，用于重新识别当时已有文件的稳定版本标识或完整文件清单 |
| 集成基线 | 当前来源操作已经接入且能够稳定重新识别的最新有效集成结果 |
| 外部规范 | 已确认的用户要求和适用于当前范围的仓库指令文件 |
| 规格确认 | 将卡片从 `draft` 转换为 `ready` 的确认行为 |
| 返工 | 规格不变的实现修正或维护使卡片从 `done` 转换为 `doing` |
| 变更影响评估 | 根据候选变化对行为契约、依赖、实施前提、验收规格、实现结果和验证依据的影响选择状态保持、修订或返工 |
| 修订 | 变更影响评估确认行为契约、依赖、实施前提或验收规格变化时将卡片转换为 `draft` |
| 完成门禁 | 卡片从 `doing` 转换为 `done` 前执行的检查和验证 |
| 验证检查点 | 执行可能改变当前候选内容的验证命令前，为当前来源操作建立的可恢复一致内容快照 |
| 验证证据 | 证明适用验证过程及最终结果的可复核命令输出、报告和差异事实，包括卡片交付所需的红—绿或前后验证证据 |
| 验证复用 | 已有验证证据与当前验证检查点候选、验证对象、命令语义、环境和判据保持一致时，直接用于一个或多个适用检查 |
| 独立上下文检查 | 在隔离上下文中仅依据冻结事实重新执行的完成检查 |
| 全树索引 | 从卡片树、外部规范、集成基线、当前候选和有效来源操作事实构造的即时逻辑视图 |
| 相关集成切片 | 从集成基线与全树索引中选取当前来源操作路径集、影响候选发现依据、实际影响作用路径、冲突关系和验证对象所需事实形成的可重识别内容与关系集合 |
| 并发候选投影视图 | 以最新共同集成基线为内容权威源，将各活动来源操作的来源未接入余差分别应用一次，用于发现组合影响和确定执行顺序且不执行目标状态分类的逻辑视图 |
| 累计影响投影视图 | 针对一个目标卡片，以最新集成事实为内容权威源，将仍与目标关联的全部已完成待累计来源最终结果作为归属差异证据，并将触发本次分类且尚未集成的来源最终投影应用一次或纳入来源取消、退出范围的关系变化后执行最终分类的逻辑视图；已经进入最新集成事实的来源结果不重复应用 |
| 独立调整操作 | 在卡片交付和删除之外，同步 CDD 结构事实且变更影响评估允许状态保持的一次原子调整 |
| 调整路径集 | 独立调整操作已经修改、移动或将在原子生效时修改，并需要共同检查的 CDD 结构文件完整路径集合 |
| 删除路径集 | 删除操作实际删除、修改或将在原子生效时修改，并需要共同检查的文件完整路径集合 |
| 来源操作 | 一次卡片交付、一次删除操作或一次独立调整操作 |
| 来源操作路径集 | 当前来源操作对应的交付路径集、删除路径集或调整路径集 |
| 初始卡片完成投影 | 交付路径集逐路径候选内容与拟写入的最终生命周期元数据组成、用于首次影响判定且尚未包含派生影响处置和来源实例转换的来源相关预期结果 |
| 卡片完成投影 | 初始卡片完成投影经过影响判定收敛后，加入拟在同一完成操作应用的影响处置和来源实例转换形成的来源相关最终预期结果，完整包含交付路径集中的规格、索引、实现、共享和测试文件内容 |
| 来源累计差异 | 同一来源操作的当前有效结果相对操作开始时集成结果的归属差异，聚合该来源已经接入的中间状态和未接入候选，抵消该来源后来恢复的变化，并排除其他来源结果；该差异用于影响判定和归属核对，不作为内容应用输入 |
| 来源未接入余差 | 来源当前投影相对最新集成结果尚未应用的归属变化，是该来源集成时唯一的内容输入；卡片交付从初始或最终卡片完成投影计算，删除操作从删除后投影视图计算，独立调整从最终调整候选与影响处置计算，已经接入的中间状态和其他来源结果不进入该余差 |
| 影响触发事实 | 来源操作中一项命中影响验证条件的前置状态、删除范围、文件边界、共享文件、存量文件、ID、路径或命令变化事实 |
| 影响来源实例 | 一个来源操作在影响验证中的稳定逻辑身份，聚合该操作的全部影响触发事实 |
| 来源结果标识 | 影响来源实例中能够稳定重识别一版候选或最终结果的标识 |
| 影响候选集 | 根据依赖图、文件关系和机器引用保守收集、等待实际影响判定的卡片集合 |
| 实际影响 | 证据确认候选变化触及目标卡片职责内的业务或技术行为契约、交付结果、实施前提或验证结论，并形成从变化到目标的具体作用路径 |
| 无影响结论 | 证据确认候选变化与目标卡片职责和验证对象保持正交，目标卡片继续满足当前状态成立条件且本次适用验证集合为空的影响判定结果 |
| 影响范围 | 从影响候选集中经实际影响判定保留、需要状态处置或适用复验的卡片集合 |
| 影响验证 | 对依赖变化、删除、文件边界、共享文件、存量文件及可能使既有验证证据失效的 ID、路径或命令调整执行影响判定、状态处置和所需复验 |
| 待影响验证 | 已达到 `ready`、`doing` 或 `done` 的卡片存在尚未处理或尚未消费的影响来源实例时的派生运行时标记 |

### 卡片树

卡片按主题目录组织为一棵树，默认卡片根目录是 `docs/`。

> **示例（非规范性）**：以下目录用于说明树结构，项目主题由实际规格确定。

```text
docs/
├── README.md
├── 基础设施/
│   ├── README.md
│   └── 仓库脚手架.md
├── 用户/
│   ├── README.md
│   ├── 手机号注册.md
│   └── 账户/
│       ├── README.md
│       └── 换绑手机号.md
└── 订单/
    ├── README.md
    └── 创建订单.md
```

卡片根目录的直属节点均为主题目录。每个主题目录恰有一个 `README.md`，并至少包含一个直属节点。直属节点可以是卡片或子主题目录。

目录位置表达当前功能归属，ID 表达当前树中的层级标识：

- 节点名称使用简短的功能名称，字符集为中文字符、英文字母和阿拉伯数字
- 普通功能名称优先使用中文；英文专有名词和标准缩写可以单独或与中文组合使用
- 主题目录索引固定使用 `README.md`
- 主题目录和卡片一级标题及 README 直属节点章节标题中的展示名称可以在中文字符与英文字母或数字的边界加入一个半角空格，移除这些边界空格后必须与节点名称一致
- 主题目录和卡片使用 ID；ID 在当前卡片树中保持全局唯一并表达主题层级
- 同一父主题目录下的卡片和子主题目录职责保持正交

节点移动或人明确要求时可以调整 ID。调整必须原子同步 YAML、README 直属节点章节标题、`depends`、「依赖」、验证命令、覆盖率路径和其他 CDD 结构文件内的机器引用，并重新执行全树 Card Lint。节点移动还必须重新执行 Card Review，确认新的 README 链保持既有功能归属、行为契约和实施前提；发生语义变化时修订为 `draft`，保持既有语义时可以保留状态和 AC 结果。路径或命令变化使验证证据失效时，重新执行对应验证。已删除或曾经使用过的 ID 可以再次使用，历史提交中的 ID 按对应提交解释。普通内容修改沿用现有 ID。

独立调整操作以一次连续处理并共同校验、原子生效的一组调整为边界。调整目标、初始范围和实现方法完成登记时操作开始；完成前在已确认边界内修正候选或同步引用仍属于同一操作，取消调整计划和全部候选变化时操作取消；一组调整完成或取消后再次开始的调整属于新操作。

调整路径集按当前卡片树、当前候选差异和 CDD 结构文件内的机器引用即时确定，由以下 CDD 结构文件组成：

- 被调整或移动的卡片 Markdown、主题目录 README 及其源路径和目标路径
- 为同步 YAML、README 索引、`depends`、「依赖」、验证命令、覆盖率路径和其他 CDD 结构文件内的机器引用而修改的卡片 Markdown 和 README
- 因本次影响处置修改或将在原子生效时修改的卡片 Markdown 和 README

独立调整操作的范围保持在 CDD 结构文件及其机器引用内，包括变更影响评估允许状态保持的文件边界声明调整；实现文件、共享文件、测试源文件或配置的内容与行为变化归入卡片交付或删除操作。每个调整路径均明确归属本次调整，路径归属和操作范围全部确定后完成高风险判定。高风险触发条件集合为空时进入实施；命中高风险条件时，先向人展示调整范围、依据、预期结果、实现方法、验证方式和失败恢复方案，取得明确确认并将确认事实纳入调整授权依据，再进入实施。实施中调整目标、调整路径集、实现方法或高风险触发事实变化时，立即暂停相关实施并返回设计阶段，原高风险判定和确认失效。更新后的完整方案仍满足独立调整边界时，重新判定并在命中高风险条件时重新取得人工确认、更新调整授权依据后继续，同一连续调整仍使用原来源操作身份；方案越出独立调整边界时，恢复到操作开始前的一致状态，取消原操作及其尚未完成的来源实例，再按目标范围建立新的卡片交付或删除操作并使用新的来源操作身份。

独立调整使用最终候选中受影响卡片声明的验证命令和覆盖率要求；变更影响评估确认失效的验证以最终候选中的命令重新建立证据。全部同步要求已经满足、需要重新执行的验证已经完成、所需验证证据已经建立且所需影响处置候选已经形成后，冻结外部规范与调整授权依据、集成基线标识、相关集成切片、调整路径集内容、来源累计差异、来源未接入余差、变更影响评估结果、影响候选集、逐目标实际影响或无影响结论、影响范围、验证证据和影响处置候选；适用验证集合为空时，验证证据集合也为空。在冻结事实上执行适用的全树 Card Lint、Card Review、验证与覆盖率结果核对、影响处置检查和外部规范符合性检查；调整命中高风险条件时，独立上下文只读取同一冻结事实并确认问题清单为空。全部检查通过且冻结事实保持一致时，使调整结果与影响处置候选共同原子生效，结果能够作为同一集成事实重新读取时操作完成。任一冻结事实或对应候选内容变化时，本轮检查失效，修正后重新冻结并检查。

应用或回读失败时按可恢复状态分流：恢复到本操作最近一个一致候选时，按原操作及其仍存在的原来源实例重新检查；恢复到操作开始前状态时，取消本操作及其尚未完成的来源实例，之后重新调整属于新操作并使用新来源实例。

### 上下文

#### README 链

当前卡片的规格上下文按以下顺序装载：

```text
外部规范
→ 当前卡片的 README 链
→ 直接前置卡片的 README 链
→ 当前卡片
→ 实现确实需要的直接前置卡片契约
→ 交付路径集中的当前实现文件
```

README 提供功能归属和节点索引，当前卡片提供交付规格，直接前置卡片补充当前实现需要使用的契约。直接前置卡片的 README 链按路径去重，装载范围为解释其功能归属和契约所需的内容。前置卡片的规格契约构成当前规格上下文；其内部实现按影响验证或兼容性判断的实际需要作为核对证据装载。

读取直接前置卡片时，先按当前卡片「依赖」中的依赖使用装载被引用的「提供」项及定义这些提供物所需的相关章节；影响验证或兼容性判断需要验证、实现记录和内部实现时再装载对应内容。

一致实现当前卡片需要更多事实时，返回设计阶段修订卡片、依赖或主题边界，将事实纳入局部完整上下文后继续。

#### 全树索引

全树索引汇集完成当前检查所需的可复核机器事实：节点和卡片事实从全部 README 与卡片提取，采用基线从外部规范提取，来源操作与待影响验证事实从集成基线、当前候选和可恢复的有效来源操作事实提取。索引至少包含：

- 节点类型、路径和 ID
- 卡片 `status`、`confirmed_by` 和 `depends`
- 卡片提供物和依赖使用
- 文件边界和文件记录
- AC 与验证文件、验证命令的映射
- 当前有效创建文件和共享文件关系
- 采用基线及其能够识别的存量文件
- 来源累计差异和来源未接入余差
- 影响候选集、逐目标实际影响或无影响结论及其证据
- 待影响验证的卡片、影响来源实例、影响触发事实、存在时的当前来源结果标识及其活动或已完成待处理状态

全树索引用于查重、检查依赖图、核对提供物和文件归属、判断开工条件、计算派生状态、确定覆盖率文件集，以及计算删除、返工、修订、共享文件和存量文件变更的影响候选集与影响范围。

全树索引按当前工作需要从最新卡片树、外部规范、集成基线、当前候选变化和当前有效影响来源实例构造，每次使用时刷新全部机器事实。影响来源实例标识用于区分来源操作，并在同一操作的候选反复变化期间保持稳定；来源操作形成候选内容后，来源结果标识用于重识别具体内容，候选内容变化时随之更新，来源完成时绑定最终结果。节点调整或重用 ID 时，既有来源实例继续使用原身份。具体标识和维护方式由外部规范或当前工具确定。

待影响验证的来源实例由当前来源操作的影响触发事实，以及已经完成但仍在等待累计分类或被受影响 `doing` 卡片消费的实例确定。来源完成后仍需保留的实例、最终结果标识和逐目标消费状态必须能够从可复核集成事实恢复；相关状态转换以能够重新识别这些事实为前提，并在事实恢复或由人确认处理方式后继续。专用索引工具属于可选实现，CDD 的成立依据是本节规定的事实判据。

#### 外部规范

已确认的用户要求和适用于当前范围的仓库指令文件共同构成外部规范。

项目规范用于补充技术栈、目录、测试、运行和发布细节，可以提高 CDD 的质量门槛，并完整保留 CDD 的核心生命周期、依赖、文件边界和验收要求。确需偏离时，由人明确批准；持续影响卡片规格或维护的例外记录在相关卡片「约束」中，仅影响本次删除操作或独立调整操作的例外记录在该来源操作的冻结授权依据中。

存量项目开始采用 CDD 时，外部规范必须记录能够稳定重新读取的采用基线。采用基线可以直接使用已有版本标识，也可以记录当时已有文件的完整仓库相对路径清单。采用基线能够唯一判断无创建记录文件的身份后继续；现有证据存在歧义时，由人确认其属于存量文件还是遗漏的 CDD 创建事实，并将结果写入相关卡片「约束」或修正对应文件记录。

存在冲突、信息缺口或无法判断的例外时，按「外部规范符合性检查」保持当前来源操作未完成，问题解决后继续。

### 依赖

`depends` 的方向恒定为“当前卡片 → 直接前置卡片”，专门表示当前卡片直接使用前置卡片的提供物；具体使用关系由当前卡片「依赖」中的依赖使用声明：

- 依赖目标可以位于卡片树中的任意主题目录
- 列表项为直接前置，传递前置由递归遍历直接边得出
- 依赖目标唯一且不指向当前卡片
- 全树卡片依赖图保持有向无环
- 列表顺序用于稳定表达，人工指令确定适用优先级
- 每个依赖目标恰有一组依赖使用，并至少引用目标卡片的一个完整提供物
- 每项依赖使用引用对应直接前置卡片实际存在的提供物

`depends` 表达能力使用关系；人工指令表达优先级或纯等待顺序。当前状态和依赖图在运行时计算可开工卡片集合及必要的偏序约束，互不依赖的卡片保持并行关系。执行资源一次仅能选择一张且人没有指定时，AI 可以从可开工集合中任选一张，依赖图继续保持真实能力关系。

当前卡片修改由另一张卡片创建的共享文件时，必须直接依赖创建卡片所提供的共享基础能力，并在依赖使用中引用该提供物。修改没有创建记录的存量文件时保持实际依赖图，其影响通过文件事实、验收和 Card Review 判断。

### 执行

#### 职责

- 人提供需求和项目事实，确认新增决策、高风险规格和例外，并抽查验收结果
- 写卡 AI 确定功能归属，完成查重和澄清，声明直接依赖、依赖使用与文件边界，并执行 Card Lint 和 Card Review
- 编码 AI 核对开工条件，按 AC 实施开发，维护实现记录，并执行完成门禁

能够从规范和当前事实唯一确定的事项由 AI 判断。存在多个合理选择，或者授权、范围、风险和业务含义无法可靠确定时，由人决策；确认结果作为当前事实继续执行。

技术选择依次遵循外部规范、当前卡片、必要的直接前置契约、仓库配置和现有工程约定。会改变外部行为、兼容性或卡片间契约的选择必须写入卡片；影响范围限于内部实现的关键选择写入「实现记录」。

Card Review 对当前检查事实形成问题清单，每个问题指向具体卡片内容、关联事实和未满足的规范要求；问题清单为空时通过，其他结果保持待处理。结果的有效范围是本次检查使用的事实，事实变化后重新执行。具体输出载体由外部规范或当前工具确定。

#### 交付路径集

交付路径集按当前卡片树、当前候选差异和实现记录即时确定，包含：

- 当前卡片的 Markdown 文件
- 因当前卡片新增或修改的卡片树 README
- 因当前卡片的影响处理已经修改或将在最终状态转换时修改的其他卡片 Markdown 和卡片树 README
- 当前卡片实际创建、修改或删除的实现文件、共享文件和测试文件
- 当前卡片实际移动文件的源路径和目标路径

交付路径集由能够明确归属当前卡片的完整仓库相对路径组成；构建文件、缓存、覆盖率报告和其他临时验收产物进入外部规范指定的临时目录。实施事实变化时立即刷新，全部路径归属确定后继续交付。

卡片 Markdown 和卡片树 README 是 CDD 结构文件，其写入权限由卡片树、README、生命周期和影响处理规则确定，并纳入交付路径集。影响处理引起的其他卡片状态和记录变化归属触发该影响的当前交付。不同卡片修改同一 README 时按集成基线顺序逐一集成，每张卡片增加或调整自身交付所需的最小索引内容。

#### 交付

一次卡片交付以一张卡片从开始处理到转换为 `done`、在尚未接入中间结果时撤销全部候选并恢复到操作开始前状态，或者按删除规则原子终止为边界。删除终止结束原卡片交付，其活动来源实例按删除规则转换为已完成待处理实例并继续逐目标消费。交付目标、初始范围和实现方法完成登记时操作开始。已经集成的 `draft` 或 `ready` 卡片继续处理时，能够重识别尚未结束的原交付则沿用原来源操作身份，能够证明没有尚未结束的原交付时开始新的卡片交付；`doing` 卡片仅沿用使其进入 `doing` 的原交付。存在原交付但其身份、状态或来源实例无法重识别时暂停相关转换，恢复可复核事实或由人确定一致处置后继续。

一次卡片交付按以下顺序执行：

1. 读取外部规范和规格上下文，通过全树索引完成查重，核对 ID、依赖、依赖使用、提供物和文件边界
2. 新建或修订入口创建或修订 `draft` 卡片，执行 Card Lint 和 Card Review；任一检查未通过时保持 `draft` 并继续设计或修订，Card Lint 通过且 Card Review 问题清单为空后才能进入规格确认；既有 `ready` 入口核对当前规格确认仍然有效；既有 `doing` 入口核对当前规格确认、实施前提和原来源操作身份仍然有效；返工入口先确认「返工」条件成立，使卡片从 `done` 转换为 `doing` 并保留有效的规格确认
3. 新建或修订入口完成规格确认，使卡片从 `draft` 转换为 `ready`；既有 `ready` 入口保持 `ready`，既有 `doing` 和返工入口继续使用 `doing`
4. 新建、修订或既有 `ready` 入口在开工条件成立后使卡片从 `ready` 转换为 `doing`
5. 按 AC 建立验证证据并完成实现，持续维护文件记录和关键决策
6. 实现形成一致状态后建立验证检查点和初始卡片完成投影，以检查点中的候选交付运行验证命令和覆盖率检查，并以初始投影对应的来源累计差异执行首次影响判定
7. 完成实现记录、验证证据、影响处置和来源实例转换，形成卡片完成投影并刷新来源累计差异、来源未接入余差、相关集成切片、交付路径集、全树索引与验证检查点；投影变化继续执行影响判定和处置，直到卡片完成投影、逐目标结论、影响范围和来源实例转换共同稳定，再冻结规格上下文、集成基线标识、相关集成切片、交付路径集内容、初始卡片完成投影、卡片完成投影、来源累计差异、来源未接入余差、实现记录、验证证据、影响候选集、逐目标实际影响或无影响结论、影响范围和影响处置候选
8. 在冻结事实上执行 Card Lint、动态检查、Card Review 和外部规范符合性检查；高风险卡片在同一事实集上增加独立上下文检查
9. 门禁全部通过后应用冻结的卡片完成投影，使当前卡片的最终生命周期元数据、影响处置、来源实例转换和交付路径集共同形成可重新读取的集成结果

其他来源操作要求一张已经集成的卡片先修订时，该卡片在自己的交付中完成所需修订并形成一致的 `draft` 检查点。`draft`、`ready` 或 `doing` 结果需要在卡片完成前接入时，统一按中间状态接入：冻结外部规范、规格上下文、集成基线、全树索引、交付路径集内容、生命周期元数据、适用的规格确认与状态转换依据、来源实例事实和影响处置候选，在冻结事实上执行当前入口条件检查、Card Lint、Card Review 与外部规范符合性检查；检查通过且冻结事实保持一致时，原子接入冻结的生命周期元数据、卡片内容、来源实例事实和适用影响处置候选，并立即回读确认实际结果只包含冻结的变化且仍通过适用检查。卡片从 `draft` 转换为 `ready` 时，基于冻结的最新全树索引重算该卡片与全部有效活动来源实例的逐目标关系，并将 `status`、`confirmed_by`、来源关系和待影响验证标记作为同一中间状态结果接入及回读。中间状态接入是仍在进行的卡片交付的可恢复中间结果，不表示卡片完成；后续状态转换继续使用同一来源操作身份。其他来源操作只接入该中间结果，不接管该卡片交付。

卡片交付撤销目标时，尚未接入的候选恢复到操作开始前状态后结束操作。已经接入 `draft` 或 `ready` 中间结果时，卡片保持当前合法状态，原交付以恢复原有效规格或形成新的有效规格为目标继续执行状态机；已经接入 `doing` 时，原规格和实施前提保持有效则撤销未接入候选、重新建立完成证据并由原交付转换为 `done`，需要恢复或改变规格时先按修订规则转换为 `draft` 并接入中间结果，再由原交付继续状态机。消费这些中间结果的来源操作按并行与影响验证规则暂停冲突范围，接入最新结果并重新判定。在卡片重新转换为 `done` 的同一完成操作中，影响触发事实为空且待处理目标集合为空时取消原来源实例；仍有影响触发事实时按最终结果标识完成原实例并由各目标消费。卡片明确保持中间状态时原交付和活动来源实例保持可重识别，之后继续处理沿用原来源操作身份。

规格问题按修订规则处理；规格不变的普通实现错误和验证失败使卡片保持 `doing` 并继续修复。

冻结表示本轮完成检查使用一组内容已经确定且能够逐项重识别的事实，具体快照格式由外部规范或当前工具选择。冻结事实必须在完成门禁、所需独立上下文检查和最终回读期间保持可用；事实内容变化、标识失效或执行上下文中断后，以能够证明仍是同一事实集为继续检查的前提，否则重新冻结并执行后续检查。

本文要求原子执行状态转换、删除或影响处置时，逻辑结果为全部相关生命周期、索引、影响和路径内容变化共同生效，或者使用验证检查点恢复到执行前的一致状态。其他来源操作接入共同生效后的完整结果。具体实现由外部规范或当前工具确定。

#### 验证检查点

运行测试、格式化、代码生成、迁移检查或其他可能改变当前候选内容的验证命令前，先核对全部候选变化的归属，刷新当前来源操作对应的交付路径集、删除路径集或调整路径集，建立验证检查点并记录候选变化基线。以下规则中的“当前路径集”指本次来源操作适用的该路径集：

- 路径集相对集成基线的增量差异必须全部归属当前来源操作，检查点精确覆盖该路径集
- 集成基线已经包含的其他来源操作内容保持其文件记录和归属；相对集成基线的其他变化完成归属和隔离后再建立检查点
- 变化基线必须能可靠识别验证命令在当前路径集内外造成的未解释变化，且不得读取、复制或暴露完成比较不需要的敏感内容；具体实现由外部规范和当前工具决定
- 构建文件、缓存、覆盖率报告和其他临时验收产物保留在外部规范指定的临时目录
- 验证命令结束后立即比较候选变化基线；除已明确列出的临时验收产物外，任何未预期差异都阻止完成并必须查明来源
- 当前路径集与检查点逐项比较，确认验证后的一致候选保持完整
- 命令有意产生的源文件变化必须经过检查，更新文件记录、验证证据和当前路径集后再刷新检查点
- 修复、返工、修订或其他候选变化使检查点失效时，完成对应验证后重新建立检查点

验证检查点用于检测和恢复已确认的意外候选破坏，其内容快照、比较和恢复方式由外部规范或当前工具确定。验证结果和来源操作完成由当前操作适用的完成检查判定：卡片交付执行完成门禁，删除操作和独立调整操作执行各自的完成规则。提交、回退或覆盖现有变化分别遵循对应授权；最终状态的检查和发布按对应来源操作的完成规则执行。

#### 验证证据

验证证据必须明确对应验证命令、集成基线、来源操作路径集内容和实际结果，并至少保留到当前来源操作的全部完成检查及所需独立上下文检查结束。删除操作和独立调整操作的最终通过结果与最终集成事实保持可复核对应，具体载体由外部规范或当前工具确定。来源操作完成前发生证据丢失或对应内容变化时，重新建立对应证据。

验证执行次数以取得当前结论所需的最小充分集合为准。已有证据同时满足以下条件时直接复用：

- 对应当前验证检查点候选中与验证对象有关的全部内容，相关内容在证据产生后没有变化
- 命令、工具版本、环境身份、外部依赖状态、测试数据前提和通过判据与当前检查一致
- 实际收集或覆盖的文件、接口、行为和错误路径完整包含当前需要验证的范围
- 原始输出、报告、退出状态和候选标识仍可复核

多张卡片需要同一命令验证同一对象时，共享一次执行结果并分别建立映射。当前来源操作已经执行的格式、静态、构建、测试、端到端或覆盖率检查满足受影响卡片的适用验证时直接复用，后续影响关系、Card Review 和分类记录引用同一证据。

命令文本存在差异时，根据命令语义和实际输出判断已有检查是否完整包含当前验证对象及判据。覆盖率证据同时保持相同的归属文件集、过滤规则、统计口径和达到当前要求的阈值。包含关系证据不足、环境变化、相关候选内容变化、证据缺失或检查失败时，执行对应的最小补充验证集合。

#### 外部规范符合性检查

外部规范符合性检查逐项核对当前来源操作路径集、操作类型对应的冻结事实、验证证据和影响处置候选是否满足当前范围适用的已确认用户要求与仓库指令文件：

- 卡片交付核对交付路径集、卡片规格和实现记录
- 删除操作核对删除路径集、删除授权依据、删除集和删除后投影视图
- 独立调整操作核对调整路径集、同步关系和调整范围

存在偏离时，只有已经由人批准并按「外部规范」记录的例外可以通过。适用要求或符合性待确认时，本次来源操作保持未完成：卡片交付按生命周期保持 `doing` 或修订为 `draft`；删除操作和独立调整操作保持或恢复到最近的一致候选。问题解决后重新执行检查。

#### 并行

并行来源操作的候选变化相互隔离，同一时刻每个来源操作由唯一执行 AI 产生变更。以下任一情况成立时，相关来源操作对冲突范围的修改、影响判定、最终状态转换和集成完成按顺序执行：

- 两个来源操作的路径集存在相同路径
- 两个来源操作的影响候选集包含相同卡片
- 一个来源操作路径集中的卡片 Markdown 对应的卡片位于另一个来源操作的影响候选集

这些冲突包括 README、共享文件、存量文件和因影响处置修改的其他卡片 Markdown。

两个来源操作命中上述冲突条件，且前一来源操作已经产生待影响验证来源时，前一来源操作先完成影响处置。删除授权和适用的高风险确认已经完成，且删除集包含前一未结束卡片交付的卡片时，按「删除」规则暂停该交付并由删除操作先行；删除取消时原交付继续，删除完成时原交付终止，其来源实例按删除规则保留待消费关系。除该特例外，后一来源操作必须先接入前一来源操作的最终集成结果，刷新集成基线、全树索引和待影响验证来源集合，再基于累计结果重新执行受影响卡片分类、验证和适用完成检查。满足无冲突条件的来源操作可以并行，并各自在自己的集成基线上执行影响验证。

任一活动来源操作的候选变化改变依赖、引用、文件关系或影响判定前提时，使用并发候选投影视图重新计算全部活动来源操作的影响候选集和冲突关系；各操作继续维护自己的候选内容。新形成的冲突按前款确定顺序。

来源操作开始和每次接入集成结果时都要确定能够重新读取相同内容的集成基线标识。集成基线可以包含已经确认归属的未提交内容；标识和快照方式由外部规范或当前工具确定。有效基线标识能够解析到同一组路径及其内容，名称、时间或描述需要与稳定内容标识组合使用。来源操作在基线能够重新读取后开始或继续。

每个来源操作进入最终集成前先读取最新集成基线，并使用并发候选投影视图比较最新相关集成切片与冻结切片，同时按「验证证据」重新核对命令语义、工具版本、环境身份、外部依赖状态、测试数据前提、通过判据和证据可复核性。切片一致、没有形成新的影响候选或冲突关系且复用条件继续成立时，既有逐目标结论、验证证据、完成检查和独立上下文检查继续有效，以最新集成基线和来源未接入余差构造预期内容；已经接入的本来源中间状态不再应用，切片之外已经完成的正交来源结果直接保留。相关切片、影响候选或冲突关系变化时，使用来源累计差异基于累计结果重新形成适用结论、证据和完成检查；复用条件变化时执行最小补充验证，并重新执行依赖对应证据的完成检查和独立上下文检查。

验证使用的构建目录、缓存、覆盖率目录、数据库、队列和其他可变资源必须在并行来源操作之间隔离。隔离方式和隔离键由外部规范或当前工具确定；使用共享可变资源的来源操作按顺序执行。

### 生命周期

#### 状态

卡片状态机固定为：

```text
          规格确认                 开工                完成门禁
draft ────────────────▶ ready ─────────────▶ doing ───────────────▶ done
  ▲                       │                    │                      │
  └─────────修订──────────┴────────────────────┴──────────────────────┘
                                               ▲                      │
                                               └─────────返工─────────┘
```

- `draft`：卡片正在设计或修订
- `ready`：规格已经确认，开工条件满足后可以实施
- `doing`：卡片正在实施或返工
- `done`：完成门禁已经通过

生命周期状态只表达卡片所处的交付阶段，不代替 Card Lint 和 Card Review 的检查结果。`draft` 可以存在待处理的设计或修订问题，任一检查未通过时保持 `draft`；Card Lint 通过、Card Review 问题清单为空且完整规格完成确认后，才能转换为 `ready`。`draft` 作为中间状态接入时，另行满足中间状态接入规则。

已经集成的卡片发生候选变化时先执行变更影响评估，并按以下条件处置状态：

- 行为契约、依赖、实施前提和验收规格保持有效，且当前状态的成立条件继续满足时，保留当前状态
- 行为契约、依赖、实施前提或验收规格发生变化时，修订为 `draft`
- `done` 卡片的规格保持有效且实现结果或验证依据需要更新时，返工为 `doing`

卡片首次转换为 `doing` 的来源状态是 `ready`。开工条件是当前卡片为 `ready`，从 `depends` 递归可达的全部前置卡片都为 `done`，并且当前卡片及该依赖闭包的待影响验证集合为空。

`doing` 卡片重新核对条件时，从待影响验证集合中排除当前卡片候选交付自身产生的来源，使其能够完成本轮影响判定。其他来源使卡片暂停实施并保留接入来源结果、Card Review、分类和失效事实处置能力；完成门禁在这些来源处理后继续。标记本身保持卡片规格，直接前置保持 `done` 是当前卡片完成的前提。

#### 规格确认

规格通常由人确认。用户明确批准卡片内容，或明确要求按展示的最终方案实施，都构成人工确认。确认对象是转换为 `ready` 时已经展示并通过 Card Lint 和 Card Review 的完整卡片规格，包括依赖、文件边界和全部 AC。

AI 在以下条件全部成立时可以确认规格：

- 卡片内容能够从已确认的外部规范和直接前置契约唯一推出
- 规格沿用既有业务规则、对外行为、兼容性和例外
- 问题清单为空
- 高风险触发条件集合为空
- Card Lint 和 Card Review 均通过
- 规格确认先于实现开始

确认后在 YAML 写入 `confirmed_by: "human"` 或 `confirmed_by: "ai"`。`confirmed_by` 对应本次已经展示和检查的完整规格。变更影响评估允许状态保持的独立调整继续沿用既有确认；调整扩大共享写入权限时由人确认并写入 `confirmed_by: "human"`。修订时按修订规则删除该字段，`draft` 卡片省略该字段，规格保持一致的返工继续保留。

#### 高风险

以下变更至少属于高风险：

- 身份认证、权限、安全或隐私
- 资金、计费或结算
- 不可逆数据操作和数据库迁移
- 改变既有公开接口兼容性
- 跨系统一致性
- 外部规范明确标为高风险的其他事项

项目可以增加高风险触发条件，并完整保留上述最低集合。高风险卡片必须由人确认规格，并在 `doing → done` 前执行独立上下文检查。

独立上下文检查在新的隔离上下文中开始，读取范围为已经冻结的规格上下文、集成基线标识、相关集成切片、交付路径集内容、初始卡片完成投影、卡片完成投影、来源累计差异、来源未接入余差、实现记录、验证证据、影响候选集、逐目标实际影响或无影响结论、影响范围和影响处置候选，过程上下文从空白状态建立。可以由同一个 AI 产品或模型执行；外部规范要求人工检查时由人执行。

独立上下文检查开始后至门禁完成前，检查结论持续对应同一冻结事实；任一冻结事实发生变化时，修正并重新执行受影响的完成门禁和独立上下文检查。门禁通过后按「完成」应用同一冻结的卡片完成投影，该步骤延续已经通过的冻结事实。

#### 返工

已确认规格、依赖和文件边界保持不变，且存在实现或完成证据需要修正，或者存在明确的纯实现维护目标时，卡片从 `done` 转换为 `doing`。纯实现维护包括保持外部行为的重构、优化，以及删除或移动当前卡片创建且已经退出有效实现的文件：

- 规格、依赖、文件边界和 `confirmed_by` 保持不变
- 全部 AC 重置为 `[ ]`
- 删除失效的验证和遗留记录
- 在「实现记录」中写明可复核的返工原因或维护目标
- 缺陷修复重新建立红—绿证据；纯实现维护使用实施前后相同验证均通过的证据
- 祖先目录的派生状态和受影响依赖方按最新事实重新计算

改动 `done` 卡片以明确的调整目标为前提，并按变更影响评估结果选择状态保持、返工或修订流程。

#### 修订

变更影响评估结果为修订时进入本流程。

修订时：

- 卡片转换为 `draft`
- 删除 `confirmed_by`
- 全部 AC 重置为 `[ ]`
- 删除失效的验证和遗留记录
- 保留正确的当前文件记录和仍然成立的关键决策
- 在「实现记录」中写明可复核的修订原因

`doing` 卡片的规格和实施前提仍然有效时，普通实现错误或验证失败继续修正并保持 `doing`。

#### 删除

删除使用独立于状态机的删除操作，结果是从卡片树移除目标节点及其能力。删除目标、初始范围和实现方法完成登记时操作开始；删除完成或按本节恢复并取消时操作结束，之后再次提出删除使用新的来源操作身份。

删除某节点使祖先主题目录的直属节点集合变为空时，将该主题目录及继续因此变空的祖先主题目录递归加入同一删除集，并把对应 README 和父 README 更新纳入删除路径集；卡片根目录始终保留。

以下规则确定删除授权：

- `draft` 且实现记录为空的卡片可以由 AI 在设计整理时删除
- 删除存在实现记录的 `draft` 卡片，或者删除 `ready`、`doing`、`done` 卡片，必须由人明确确认能力、文件和依赖范围
- 删除主题目录时，其整棵子树属于同一删除集；删除集中存在非空实现记录时必须由人确认

删除集命中任一人工确认条件时必须由人确认。AI 自行确认删除需要同时满足：删除集中的卡片全部为实现记录为空的 `draft`，存量文件集合为空，影响范围内保留的 `ready`、`doing` 或 `done` 卡片集合为空，已确认规格保持不变，并且其他人工确认触发条件集合为空。

删除准备开始前完成高风险判定。删除命中高风险条件时，先向人展示删除范围、依据、预期结果、实现方法、验证方式和失败恢复方案，取得明确确认并将确认事实纳入删除授权依据。全部删除授权条件和适用的高风险确认完成后进入准备。准备或正式删除期间，在已确认边界内由依赖方准备和候选修正产生的删除集、删除路径集或影响范围更新按最新事实继续处理；删除集、删除路径集或影响范围超出已确认边界，或者实现方法、高风险触发事实或删除授权条件变化时，立即暂停相关操作并返回设计阶段，原删除授权、高风险判定和确认失效，基于更新后的完整方案重新执行删除授权与高风险判定，取得全部适用确认并更新删除授权依据后继续。

删除取消按当前阶段恢复一致结果：

- 准备开始前取消时，撤销删除计划和本操作候选，恢复到操作开始前状态并结束操作；此阶段尚未建立删除来源实例
- 准备期间取消时，撤销尚未接入的删除候选并以最新集成结果结束删除操作；依赖方已经原子接入的 `draft` 检查点继续属于各自卡片交付，其后续规格确认、继续修订或恢复由对应卡片交付处理；删除集内暂停的卡片交付按原身份和来源实例继续
- 正式删除开始后、删除完成前取消时，从当前正式删除阶段的删除前检查点恢复全部删除路径集内容和本操作影响处置候选，回读确认与该阶段开始前的集成结果一致；删除候选、影响触发事实和待处理目标集合均为空后取消活动来源实例并结束操作，已经接入的依赖方 `draft` 检查点继续按上一项处理，删除集内暂停的卡片交付按原身份和来源实例继续
- 删除完成后的能力恢复作为新的卡片交付、独立调整或其他适用来源操作设计和实施，并使用新的来源操作身份

删除集内存在未结束的卡片交付时，删除操作与这些交付按路径冲突串行。正式删除开始前，相关卡片交付暂停普通处理，撤销其尚未接入的候选，并将已经接入的中间状态、原来源操作身份、活动来源实例和逐目标关系纳入删除准备事实；删除授权覆盖这些候选的撤销。删除影响范围包含这些来源实例仍在等待处理的全部目标。删除完成前原交付及其来源实例保持可重识别且处于暂停状态；删除取消时按前款继续。

本次删除从正式开始删除时登记为待影响验证来源；开始前先完成准备。所有直接依赖删除集的保留卡片都分别在自己的交付中修订为 `draft`，移除指向删除集的依赖边，执行 Card Lint 和 Card Review，并按前置到依赖方的顺序接入结果。递归影响候选集内其他 `doing` 卡片暂停普通实施；删除后投影视图实际改变其行为契约、实施前提或验收规格时修订为 `draft`，保持这些事实时按 `doing` 卡片的影响分类继续原交付。传递依赖边在修订后的规格需要变化时同步调整。

准备开始时记录待修订卡片集合，包括直接依赖删除集的保留卡片，以及删除后投影视图实际改变其行为契约、实施前提或验收规格的其他 `doing` 卡片。每次接入后重新计算删除集、删除路径集、影响候选集和影响范围，并按同一条件刷新待修订集合；卡片修订为 `draft` 并接入后移出。指向删除集的保留依赖边集合和待修订集合均为空后开始删除。

删除前确定删除集、完整删除路径集和影响范围；现有证据存在歧义时由人确认。删除路径集包含删除集中的节点文件、需要更新的 README、删除操作内形成影响处置候选的保留卡片 Markdown、删除集中卡片创建的文件，以及需要移除相关内容的共享文件和存量文件。变更前按「验证检查点」建立并保留删除前检查点直至删除操作结束；形成删除后候选时另行刷新用于验证候选的检查点。

正式删除开始后新增删除路径时，先暂停正式删除，从当前删除前检查点恢复本操作已经形成的删除路径集变化和影响处置候选，接入此前无冲突且已经完成的来源操作结果，并返回准备阶段基于最新集成结果重新确定完整删除路径集。更新后的范围仍在已确认边界内时，建立覆盖完整路径集的新删除前检查点并开始新的正式删除阶段；超出边界时按删除授权与高风险规则重新确认后继续。同一删除目标和连续处理仍使用原删除操作身份。

删除集中每张原创建卡片继续作为其创建文件的权限主体。删除授权覆盖已确认范围内由这些卡片创建的文件，其他文件继续由原权限主体维护。

删除后的当前候选内容必须满足：

- 删除集中的节点、索引和依赖引用已经移除
- 每个保留主题目录至少包含一个直属节点
- 删除卡片创建的全部文件；保留能力需要其中内容时，先由保留卡片创建具有明确归属的替代文件并更新引用
- 从共享文件和存量文件中移除仅属于删除能力的组合或配置内容
- 原依赖方已纳入删除集，或已修订为使用仍然存在的提供物
- 保留卡片的规格、文件和验证仍然自洽

删除后的 ID 可以在当前树中重新使用。删除涉及存量文件时必须由人确认，并执行对应影响验证。

删除后完成保留卡片所需验证和影响处理，形成所需影响处置候选。影响分类使用本次删除、删除集内终止交付及其活动实例全部完成后的投影视图，并将这些同步完成的实例视为同一分类结束组，各来源实例身份继续保持独立；累计影响投影视图以最新集成事实为内容权威源，各同步完成结果仅提供逐来源归属差异证据，尚未集成的删除结果应用一次。该组结束后没有其他活动来源的 `ready` 或 `done` 卡片在冻结前完成最终分类并形成关系清除结果或完整处置候选，仍有其他活动来源时保留待累计关系。全树索引同时形成所有有效来源实例中目标位于删除集的逐目标关系清除候选。然后冻结外部规范与删除授权依据、集成基线标识、相关集成切片、删除集、删除路径集、来源累计差异、来源未接入余差、影响候选集、逐目标实际影响或无影响结论、影响范围、删除后投影视图、累计影响投影视图、验证证据、影响处置候选、指向删除集的逐目标关系清除候选，以及删除集内未结束卡片交付及其来源实例的处置候选。

以冻结事实检查上述删除后条件、剩余卡片树的 Card Lint 与 Card Review、外部规范符合性、同步完成实例组的累计分类、指向删除集的全部逐目标关系清除候选、删除集内未结束交付的来源实例处置，以及删除路径集之外是否存在未解释变化。每个被终止交付的最后有效来源结果及其标识必须能够从冻结事实稳定重识别；该条件待确认时删除保持未完成。删除命中高风险条件时，独立上下文只读取同一冻结事实，确认问题清单为空。任一冻结事实或对应候选内容变化都会使本轮检查失效，修正后重新冻结并检查。

全部检查通过后，原子应用冻结的非 `doing` 卡片影响处置候选，并清除所有有效来源实例中目标位于删除集的逐目标关系；各实例面向其他目标的关系保持不变，关系清空的已完成实例按生命周期退出全树索引。删除集内未结束卡片交付的原来源实例保留自身稳定身份、影响触发事实和分类证据，将冻结的最后有效来源结果标识绑定为自身终止最终结果标识，并转换为已完成待处理实例；面向保留卡片且未由本次最终分类清除的关系继续保留。随后终止这些卡片交付，删除来源实例独立承载删除触发事实，使删除路径集、各来源实例和逐目标关系共同形成可重新读取且没有孤立来源的集成结果。回读结果与冻结事实一致时删除完成。应用或回读失败时从验证检查点共同恢复卡片、来源实例和逐目标关系的一致状态。Git 差异和历史可以保存文件事件。

#### 影响验证

影响来源实例按来源操作聚合：

- 同一次卡片交付使用一个实例聚合其在本节命中的全部影响触发事实
- 同一次删除操作在按「删除」规则完成准备并开始删除时建立一个实例，后续删除集和删除路径集变化持续更新该实例
- 同一次独立调整操作至多建立一个实例
- 同一实例的影响候选集是其全部有效影响触发事实分别计算结果的并集；影响范围是候选集内逐目标实际影响判定结果的并集，每个目标同时记录使其进入候选集的触发事实，以及实际影响与作用路径或无影响依据及其判定证据
- 每个来源操作使用独立实例；同一卡片新的交付形成影响触发事实时建立新实例，并保留该卡片上一次交付中仍在等待其他卡片消费的实例

影响来源实例遵循以下生命周期：

1. 来源操作的影响触发事实集合首次变为非空时建立活动实例；触发事实集合为空时保持无活动实例
2. 同一来源操作的候选内容、删除范围或触发事实增加、改变或部分撤销时持续更新原实例、来源结果标识、影响候选集和影响范围；任一变化使既有实际影响判定、处置候选或证据的依据变化时，对应结果失效并按最新事实重新执行；活动实例重新计算后，逐目标关系与最新影响范围中处于 `ready`、`doing` 或 `done` 的卡片保持一致，`draft` 卡片按分类规则处理，具有有效无影响结论的候选卡片保持原状态并完成对当前实例的处理
3. 来源操作按对应完成规则生效时，活动实例转换为已完成待处理实例并绑定最终结果标识；卡片交付按删除规则终止时，将冻结且可重识别的最后有效来源结果标识绑定为原活动实例的终止最终结果标识，并将实例转换为已完成待处理实例，结果或标识无法重识别时删除保持未完成；完成失败、恢复检查点或继续修正候选时，至少一项影响触发事实仍然存在则按最新事实更新原活动实例，全部触发事实消失时按下一项处理
4. 来源操作完成前撤销全部影响触发事实，且候选或最终结果的待处理目标集合为空时取消实例；撤销部分触发事实时更新原实例；同一来源操作之后再次形成影响触发事实时继续使用该操作原有的稳定实例身份
5. 已完成实例按受影响目标分别累计或消费；某个目标完成处理后清除该目标与实例的关系并保留其他目标关系，全部目标关系清除后实例退出全树索引

每张待影响验证卡片按影响来源实例的稳定标识维护去重后的触发来源集合，并区分活动来源与已完成待处理来源。同一卡片先后产生的来源操作分别使用独立实例。活动来源包括尚未完成的卡片交付、删除操作或独立调整操作实例；已完成待处理来源包括等待累计分类的来源实例和等待受影响 `doing` 卡片消费的来源实例。两类来源均为空时解除标记。

任一前置卡片处于 `draft`、`ready` 或 `doing` 时，其非完成状态使依赖方的当前开工条件或普通实施条件保持未成立，并构成影响触发事实；全树索引为该前置的当前卡片交付建立或更新来源实例，沿 `depends` 反向递归收集影响候选集。前置从 `done` 转换为 `doing` 或 `draft` 时立即刷新该实例和候选集，再按依赖方当前状态处理：

- `draft` 卡片保持 `draft`，后续规格确认前按最新前置事实检查
- `ready` 卡片保持 `ready`，标记清除后恢复开工资格
- `doing` 卡片暂停普通实施，并在来源完成后按本节通过自己的交付处理
- `done` 卡片保留既有状态和完成记录，标记清除后恢复依赖方开工和目录派生 `done` 所需的有效完成资格

前置卡片准备重新转换为 `done` 时，基于其来源累计差异逐目标执行影响判定。最终结果未命中本节任一实际影响条件，并有证据确认目标职责和验证对象保持原状时形成无影响结论，依赖方清除当前实例关系并沿用既有卡片内容和验证结果；最终结果造成实际影响时执行适用验证和处置。当前来源实例的全部逐目标结论、验证和来源完成前处置达到本节完成条件后，该实例与前置卡片的 `doing → done` 转换作为同一完成操作生效；需要累计分类或由受影响 `doing` 卡片消费的关系按下文继续保留，其他来源实例也继续保留。前置进入删除范围时转由删除规则处理。

以下情况形成影响触发事实或刷新对应实例，并执行影响验证：

- 前置卡片从 `done` 转换为 `doing` 或 `draft` 时，其非完成状态立即形成影响触发事实；返工或修订后准备重新完成时，以候选交付结果刷新同一实例
- 删除节点或能力
- 调整文件边界声明
- 实际修改、删除或移动共享文件
- 实际修改、删除或移动存量文件
- ID、路径或命令调整导致既有验证证据可能失效

影响候选集以卡片 ID 集合表示，按以下规则保守收集、合并并去重：

- 前置变化从变化卡片沿 `depends` 的反向索引递归收集直接和传递依赖方
- 删除从删除集沿反向依赖递归收集保留依赖方
- 文件边界声明调整收集创建卡片、记录同一路径的其他卡片及对应共享基础能力的依赖方，再沿这些卡片的反向依赖递归收集依赖方
- 共享文件变更先收集创建卡片、记录同一路径的其他卡片及共享基础能力的依赖方，再沿这些卡片的反向依赖递归收集依赖方
- 存量文件变更先收集记录同一路径的其他卡片，并通过代码调用或引用搜索、配置引用和提供物语义识别依赖其既有行为的卡片，再沿这些卡片的反向依赖递归收集依赖方
- ID、路径或命令调整收集引用调整对象且既有验证证据可能因此失效的卡片，再沿这些卡片的反向依赖递归收集依赖方

结构关联承担完整发现候选的职责，实际作用路径承担确认影响范围和验证需求的职责。同一共享文件、同一创建卡片、同一宽泛提供物或反向依赖链关系先形成候选，再由以下实际影响判定确定处理方式。

影响候选集建立后，逐张卡片根据验证检查点中的来源累计差异执行实际影响判定。满足以下至少一项的候选卡片进入影响范围：

- 候选变化触及目标卡片实际使用的提供物、输入输出、错误、安全、数据、状态、兼容性或其他行为契约
- 候选变化进入目标卡片实际使用的编译、链接、配置、启动、运行、迁移、部署或测试路径，使对应交付结果需要重新确认
- 候选变化修改目标卡片拥有或记录的文件、验证命令、覆盖率范围或其他完成依据，使既有验证结论与当前事实需要重新对应
- 前置最终候选、删除或其他变化使目标卡片的实施前提或能力可用性直接失效

实际影响判定必须指出从具体差异到目标职责的作用路径，并使用代码引用、构建或配置关系、运行调用链、提供物使用、验证覆盖范围或实际输出作为证据。新增模块、依赖或测试与目标职责保持正交，并且目标的导入、注册、调用、配置和打包路径保持原状，且未命中上述任一实际影响条件时，形成无影响结论；创建或装配该变化的基础卡片按其实际构建和运行边界判断。

无影响结论由来源操作的影响证据记录目标卡片、候选触发事实、目标职责和验证对象保持原状的依据及所用证据。对应卡片沿用已集成的 Markdown 内容、生命周期、AC、实现记录和验证记录，本次面向该目标的适用验证集合为空，并完成对当前来源实例的处理。来源操作路径集保持由自身交付和实际影响处置组成。候选变化后重新检查结论依据；证据不足且结论会改变状态或验证范围时，形成具体问题并取得明确结论。

每个候选目标的影响证据保留一项当前有效结论，内容为目标、触发事实、实际影响与作用路径或无影响依据、所用证据，以及适用验证或复用映射。需要状态处置的目标按对应生命周期规则更新自身卡片；无影响目标沿用已集成卡片内容。

来源操作为卡片交付时，来源卡片自身的规格、实现和证据由当前完成门禁检查，影响候选集和影响范围从其他卡片建立。

文件边界声明、共享文件、存量文件或者 ID、路径或命令调整的影响候选集完整，逐目标实际影响或无影响结论可靠，且影响范围具有验证方案，是来源操作的完成前提；现有证据存在歧义时由人确认。

来源卡片的候选交付需要修正以满足自身规格或既有提供物契约时，问题归属来源卡片并使其保持 `doing`；依赖方继续使用既有真实契约。

当前卡片修改共享文件后，创建该共享文件的直接前置卡片必须继续满足既有规格、提供物和 `done` 条件。直接前置需要返工或修订时，当前卡片修订为 `draft`，并在前置卡片恢复有效实施前提后继续。

影响分类使用当前来源操作中全部冻结实例状态转换完成后的投影视图；删除来源实例使用删除完成、删除集内终止交付的实例转换完成后的投影视图。核对 `ready` 卡片开工条件时，将本次完成操作原子清除的来源实例组视为已经处理，其他待影响验证来源实例和累计集成事实继续生效。

影响范围内所有保留卡片按前置到依赖方的拓扑顺序处理。验证范围由实际作用路径确定，证据集合需要补充时执行对应契约、实施前提或既有完成依据的命令和覆盖率检查；实际影响覆盖整卡或系统边界保持整体责任时覆盖该卡片全部验证。已有证据按「验证证据」复用，来源操作的影响证据记录本轮执行或证据复用的最终映射：

- `draft` 卡片保持 `draft`，并且不保留逐来源目标关系；卡片已经是 `draft` 或影响处置使其转换为 `draft` 时，原子清除该卡片与全部当前来源实例的关系，后续规格确认前基于最新全树事实重新执行 Card Lint 和 Card Review
- `ready` 卡片重新执行 Card Review 并核对开工条件，对保持 `ready` 或修订为 `draft` 进行分类
- `doing` 卡片暂停普通实施，按并行规则确定串行顺序；来源先完成时，该卡片随后接入来源结果，并在自己的交付中重新执行 Card Review，决定保持 `doing` 还是修订为 `draft`，再重置受影响的 AC、删除失效验证记录并执行所需验证
- `done` 卡片执行适用验证命令、适用覆盖率检查和 Card Review，对保持 `done`、修订为 `draft` 或返工为 `doing` 进行分类

来源完成时，受影响 `doing` 卡片与该来源实例的关系转换为已完成待消费关系，并保留实例及其最终结果标识。保留事实必须能够在新的执行上下文中从可复核集成事实恢复，具体维护方式由外部规范或当前工具确定。卡片接入与来源冻结候选一致的结果，在自己的交付中完成上述影响处理后，原子清除当前卡片与已经消费实例的关系；其他来源实例继续维持暂停，来源实例集合为空时恢复普通实施和完成门禁。

`ready` 或 `done` 卡片在当前来源实例处理后仍有活动来源实例时，当前分类形成待复核结论，状态、AC、验证记录和遗留记录保持当前事实；当前来源实例及其最终结果标识转为已完成待累计分类来源实例。当前卡片的最后一个活动来源关系因来源完成、取消或退出影响范围而结束时，造成该关系结束的来源操作触发最终分类；分类基于最新累计影响投影视图，处理仍与当前卡片关联的全部已完成待累计来源实例。最后一个活动来源完成且仍在当前卡片影响范围内时，其实例转换为已完成待累计来源并加入该组；来源取消或退出影响范围时，实例组由其余仍保持关系的来源实例组成。

最终分类按生命周期中的变更影响评估条件形成状态保持、修订或返工结果。得到最终通过结论或完整影响处置候选后，原子清除当前卡片与该来源实例组的关系。

影响处置候选包含将要应用到受影响非 `doing` 卡片的精确状态、AC 和实现记录变化，必须满足生命周期与 Card Lint。卡片交付产生候选时将对应卡片路径纳入交付路径集；门禁检查候选最终内容，通过后候选与来源状态转换共同形成可重新读取的集成结果。删除操作按「删除」的最终步骤应用候选；独立调整操作按其完成规则与调整结果共同原子应用候选。候选生效后，受影响卡片的后续规格确认、实施和验证属于自己的交付。

当前卡片完成以影响候选集完整、全部逐目标实际影响或无影响结论成立，以及影响范围内所需验证和处置已经确定为前提。处理当前来源实例后仍有活动来源实例的 `ready` 或 `done` 卡片形成待复核结论；当前来源实例是最后一个活动来源实例时，保持现状的非 `doing` 卡片必须在累计影响投影视图中完成适用复验，需要后续工作的非 `doing` 卡片必须具有完整处置候选。受影响的 `doing` 卡片必须已经暂停并确定串行顺序，且每个影响问题均具有分类。来源操作的候选内容、删除范围或影响触发事实变化并使既有判断依据失效时，原实际影响与无影响结论、处置候选和证据失效并重新执行。任何状态变化都会使依赖方的开工条件和目录派生状态按最新事实重新计算。

## README

README 说明目录节点的整体功能并索引直属节点；依赖由卡片维护，目录状态按本节规则即时派生。

### 格式

#### 共用规则

- YAML 头必须位于文件开头且恰有一段
- 所有 YAML 标量使用双引号字符串
- `created`、`updated` 使用有效的 `YYYY-MM-DD`，并满足 `updated >= created`
- `created` 写入后保持稳定，README 内容变化时刷新 `updated`
- YAML 之后依次写一级标题、整体功能描述和直属节点章节

#### 卡片根目录

卡片根目录 `README.md` 的字段集合和顺序固定为 `project`、`created`、`updated`。

一级标题格式固定为 `# <项目名称>`，项目名称与 YAML 中的 `project` 完全一致。

> **示例（非规范性）**

```markdown
---
project: "demo"
created: "2026-07-21"
updated: "2026-07-21"
---

# demo

demo 提供用户身份与订单管理能力。

## USER：用户

提供用户身份建立与维护能力。
```

#### 主题目录

主题目录 `README.md` 的字段集合和顺序固定为 `id`、`created`、`updated`。

主题目录 ID 的每个片段必须匹配 `[A-Z][A-Z0-9]*`。顶层主题目录使用单个片段；子主题目录 ID 为“父主题目录 ID + `-` + 本级片段”。

一级标题格式固定为 `# <展示名称>`。YAML 维护当前主题 ID，一级标题承载由主题目录名称生成的展示名称。

> **示例（非规范性）**

```markdown
---
id: "ORDER"
created: "2026-07-21"
updated: "2026-07-21"
---

# 订单

提供订单创建及订单生命周期管理能力。

## ORDER-01：创建订单

根据用户提交的信息生成并保存订单。
```

### 正文

一级标题和整体功能描述始终必填。整体功能描述使用恰好一个叙事段落，说明当前项目或主题提供什么功能及其整体边界。

每个直属节点恰占一个 `## <ID>：<展示名称>` 章节。ID 与对应节点 YAML 完全一致；展示名称与对应节点一级标题中的展示名称完全一致。章节内容恰为一个功能描述叙事段落。

功能描述聚焦对外提供的功能边界。卡片描述概括其全部提供物及作用；主题目录描述聚合整个子树。章节顺序用于阅读和展示，依赖、开工顺序及人工选择分别由对应机器事实和人工指令表达。

### 派生状态

目录节点状态在需要时从直属节点即时计算：

1. 直属节点非空、全部派生为 `done`，子树中的待影响验证卡片集合为空，且当前 README 的 Card Lint 和 Card Review 通过时，目录节点派生为 `done`
2. 至少一个直属节点为 `doing` 或 `done` 时，派生为 `doing`
3. 直属节点状态属于 `draft` 或 `ready`，且至少一个直属节点为 `ready` 时，派生为 `ready`
4. 其他情况派生为 `draft`

子主题目录先计算自身派生状态，再参与父目录计算。卡片根目录的直属节点集合为空时派生为 `draft`。新增、删除、移动、重命名或修订节点后，按最新树即时重算，README 继续维护功能和索引事实。

### 检查

#### Card Lint

至少检查：

- YAML 头数量、字段集合、字段顺序、值类型和日期
- 主题目录 ID、父子层级和当前树唯一性
- 目录和节点命名、卡片根目录及主题目录一级标题，以及直属节点章节的 `ID：展示名称` 格式
- 整体功能描述和直属节点章节结构
- 直属章节与实际直属节点一一对应，标题中的 ID 和展示名称完全一致
- README 不包含 `status`、`confirmed_by` 或 `depends`

#### Card Review

至少判断：

- README 链是否完整承载项目和主题边界
- 整体功能描述是否准确并保持在功能边界层级
- 父 README 中的主题描述是否与子主题整体功能一致
- 直属节点描述是否完整、真实并保持提供物含义
- 兄弟节点职责是否正交
- 主题拆分是否让卡片保持局部完整和最小上下文
- 英文节点名称是否为恰当的专有名词或标准缩写

## 卡片

### 格式

卡片使用封闭格式。二级章节的固定顺序是：

```text
提供 → 依赖 → 目标 → 术语 → 规则 → 约束 → 交互 → 界面 → 状态
→ 数据 → 运行 → 异常 → 文件 → 验证 → 验收 → 实现记录
```

每种二级章节至多出现一次。必选章节是「提供」「目标」「文件」「验证」「验收」；其他章节按出现条件使用。

> **示例（非规范性）**：以下卡片用于解释结构；项目要求来自规范正文、外部规范和实际卡片，覆盖率阈值用于演示 CDD 的默认值。

````markdown
---
id: "USER-01"
status: "draft"
created: "2026-07-21"
updated: "2026-07-21"
depends:
  - "INFRA-01"
---

# 手机号注册

## 提供

- 接口：POST /api/register
- 能力：手机号注册

## 依赖

- INFRA-01
  - 能力：HTTP 路由装配

## 目标

新用户使用手机号和验证码建立账户。

## 规则

- 手机号在判重和存储前统一标准化
- 同一标准化手机号最多对应一个账户

## 文件

实现文件：

- src/user/register/

共享文件：

- src/routes.py

测试文件：

- tests/e2e/user/register_test.py

## 验证

验证项：

- AC-01
  - 文件：tests/e2e/user/register_test.py
- AC-02
  - 文件：tests/e2e/user/register_test.py

验证命令：

- pytest tests/e2e/user/register_test.py --cov=src/user/register --cov-report=json:coverage/USER-01/coverage.json

覆盖率：

- 报告：python；coverage-json；coverage/USER-01/coverage.json
- 行覆盖率：>= 90%
- 函数覆盖率不适用：Python；本项目的 coverage.py 报告不提供函数或方法覆盖率

## 验收

- [ ] AC-01 正常注册
  - Covers
    - 接口：POST /api/register
    - 能力：手机号注册
  - Given
    - 手机号尚未注册且验证码有效
  - When 提交手机号和验证码
  - Then
    - 返回新账户标识
    - 保存标准化手机号
- [ ] AC-02 拒绝重复注册
  - Covers
    - 能力：手机号注册
  - Given
    - 标准化手机号已经存在
  - When 再次提交同一手机号
  - Then
    - 返回重复注册结果
    - 账户记录保持不变
````

### 元数据

卡片 YAML 字段按以下顺序排列：

| 字段 | 出现条件 |
| --- | --- |
| `id` | 必填 |
| `status` | 必填 |
| `created` | 必填 |
| `updated` | 必填 |
| `confirmed_by` | `ready`、`doing`、`done` 时必填 |
| `depends` | 存在直接前置时使用 |

- 所有标量值和 `depends` 列表项使用双引号字符串
- `status` 的取值为 `draft`、`ready`、`doing`、`done`
- `confirmed_by` 的取值为 `human` 或 `ai`
- `created`、`updated` 使用有效的 `YYYY-MM-DD`，并满足 `updated >= created`；`created` 写入后保持稳定，内容或状态变化时刷新 `updated`
- 卡片 ID 为“完整主题目录 ID + `-` + 两位序号”，序号范围为 `01`–`99`
- 新卡片使用当前主题中未占用的最小序号；可用序号集合为空时拆分子主题
- `depends` 按 ID 字典序排列

一级标题格式固定为 `# <展示名称>`。ID 仅由 YAML 的 `id` 维护；展示名称由卡片无后缀文件名生成，并与父 README 章节标题中的展示名称一致。

### 正文

#### 通用规则

| 章节 | 出现条件 | 内容边界 |
| --- | --- | --- |
| `提供` | 必选 | 当前卡片拥有的输出契约 |
| `依赖` | 存在 `depends` 时必选 | 当前卡片对每张直接前置卡片提供物的精确使用关系 |
| `目标` | 必选 | 当前交付的结果与适用范围 |
| `术语` | 有领域词歧义时 | 当前卡片需要定义的领域名词 |
| `规则` | 有业务规则时 | 计算、判定、资格、优先级、幂等和并发 |
| `约束` | 有硬指标或技术边界时 | 结构、性能、安全、合规、兼容和版本要求 |
| `交互` | 有正常操作路径时 | 用户或系统的连续正常步骤 |
| `界面` | 有界面要求时 | 布局、视觉、文案、状态和无障碍 |
| `状态` | 有业务实体状态转换时 | 实体生命周期和转换结果 |
| `数据` | 有数据契约时 | 接口、表、函数、存储、事件和权限 |
| `运行` | 有运行增量时 | 配置、命令、迁移、发布、回滚和可观测性 |
| `异常` | 有失败场景时 | 失败触发条件和安全结果 |
| `文件` | 必选 | 预定写入边界 |
| `验证` | 必选 | AC 与验证方式、命令和覆盖率 |
| `验收` | 必选 | 可执行 AC |
| `实现记录` | 产生实施事实后 | 文件、验证、决策和当前轮次事实 |

「目标」使用一个叙事段落。「术语」「规则」和「界面」使用一层无序列表。「交互」使用一层有序列表。「状态」使用列名为 `当前状态`、`触发条件`、`目标状态`、`结果` 的表格。

「目标」「规则」「约束」「交互」「界面」「状态」「数据」「运行」和「异常」中的每项可验证要求，都必须由至少一条 AC 断言。

### 提供

「提供」每行写一个规范化标识。类型集合和顺序固定为：

```text
接口、页面、表、组件、命令、存储、事件、制品、能力
```

- 每行格式为 `- <类型>：<稳定标识>`
- 同一类型按完整提供物字典序排列
- 清理首尾空白后的整行是提供物唯一键
- 同一提供物在全树中具有唯一声明卡片
- 其他输出类型统一归入「能力」
- 声明制品时，「运行」成为必选章节并写明位置和版本规则

### 数据

「数据」以对象为单位声明当前卡片需要维护的数据契约。第一层对象类型集合为 `接口`、`表`、`函数`、`存储`、`事件`、`权限`。同一对象的属性保持唯一，并完整说明当前实现和验收需要的结构、行为和边界。

每张表由提供该表的唯一卡片维护结构。使用其他卡片拥有的表时，通过 `depends` 和「依赖」中的提供物引用建立使用关系。数据库产品、版本、类型、主键策略、迁移、注释和并发规则由外部规范或当前卡片「约束」确定，CDD 保持技术中立。

> **示例（非规范性）**：以下 PostgreSQL 片段用于演示一种可选的数据表达；项目采用的数据库、标识、字段和版本由外部规范或卡片确定。

```markdown
- 表：users
  - 用途：用户账户
  - 字段：
    - user_id：UUID NOT NULL
    - phone：TEXT NOT NULL
  - 主键：user_id
  - 唯一：phone
```

项目选择其他数据库或数据系统时，使用项目外部规范规定的等价结构。Card Lint 检查当前项目已经确认的结构规则，规范要求来源为正文和已确认项目事实。

### 约束

「约束」可以按以下固定分组表达适用内容：

```text
目录结构 → 技术栈版本 → 工具链版本 → 运行环境版本 → 约束要求
```

- 目录结构使用 `text` 代码块
- 版本分组使用两列表格，明确技术或工具及版本范围
- 约束要求使用一层无序列表
- 分组按适用条件出现
- 降低 CDD 默认质量门槛或偏离核心规则时，逐项写明原因和人工批准范围

### 运行

「运行」记录当前卡片相对于项目既有运行条件新增或改变的要求，可以按以下固定分组表达：

```text
运行配置 → 运行命令 → 运行要求
```

运行配置使用两列表格；运行命令使用从仓库根目录执行的命令列表；运行要求使用一层无序列表。各分组按适用条件出现。

### 异常

异常格式固定为 `- EX-两位编号：触发条件和安全结果`。编号从 `EX-01` 连续递增并保持唯一。超过 `EX-99` 表示卡片需要拆分。

### 文件

「文件」固定使用以下分组顺序：

```text
实现文件 → 共享文件 → 测试文件
```

- 「实现文件」在当前卡片创建实现文件或修改非共享存量文件时使用，允许仓库相对目录或完整文件路径
- 「共享文件」在当前卡片创建或修改共享落点时使用，路径形式为完整文件路径
- 「测试文件」存在测试代码时使用，允许仓库相对目录或完整文件路径
- 「实现文件」和「共享文件」至少出现一个
- 目录路径以 `/` 结尾；完整文件路径以文件名结尾
- 路径使用可确定的仓库相对规范形式；禁止绝对路径、通配符、`.`、`..` 和值无法确定的变量
- 符号链接解析结果必须仍位于声明边界内
- 每条路径归入一个分组；按下文规则共享的测试文件同时归入「共享文件」和「测试文件」

构建文件、缓存、覆盖率报告和其他临时验收产物使用外部规范规定的目录；覆盖率报告目录缺少外部规定时，默认使用 `coverage/<卡片ID>/`。这些产物作为临时验收产物独立于文件边界和文件记录；目录位于仓库内时由版本控制忽略。并行来源操作的临时输出遵守统一的资源隔离规则。

实现目录是当前卡片允许创建文件或修改存量文件的范围。不同卡片的实现目录可以重叠或嵌套；实际创建文件的完整路径在全树中具有唯一归属卡片。

当前卡片首次创建文件时，该文件归当前卡片维护。创建卡片维护普通创建文件；将完整路径声明为共享文件后，直接依赖该共享基础能力的卡片获得按声明修改该文件的权限。

共享文件用于跨卡片组合或项目级配置，包括路由注册、插件装配、模块导出、依赖清单、锁文件、构建配置和 CI 配置；卡片独有的业务实现由该卡片创建的内聚文件承载。创建共享文件的卡片在「提供」中声明相应的共享基础能力；多个共享文件可以共同实现同一个提供物。其他卡片修改共享文件时直接依赖创建卡片，并产生完成当前交付所需的最小变化。

创建卡片将既有非共享文件开放给其他卡片修改时先执行变更影响评估。文件内容和路径保持不变、归属和验证依据保持有效，且创建卡片已经提供对应共享基础能力时，通过独立调整将完整路径声明为共享文件并保持当前状态；共享写入权限扩大由人确认。依赖方声明该提供物的依赖使用和自己的共享文件边界。共享文件职责边界成立时执行该转换；需要承载卡片独有职责时，由拥有对应职责的卡片创建新的内聚文件并调整调用关系。

跨卡片共同维护的测试装配、夹具或端到端场景文件可以作为共享文件，同时保留在创建卡片的测试文件边界中。依赖方修改时在自己的「共享文件」和「测试文件」中声明同一完整路径；测试文件归创建卡片所有，依赖方记录为修改文件。本次适用验证集合由当前新增场景和实际作用路径触及的既有场景组成；其余既有场景通过全树索引中的 AC 与验证文件、验证命令映射定位相关候选卡片，现有证据确认候选变化与对应验证对象正交时形成无影响结论并沿用原验证。普通单卡测试文件在所属卡片内维护。

存量文件由采用基线识别，其身份独立于 CDD 创建记录。当前卡片可以在实现边界内修改存量文件，并由 AC、验证和 Card Review 覆盖。新增行为优先放入当前卡片创建的文件；当前卡片覆盖率计算采用其归属的创建文件和共享文件。

创建文件的卡片拥有该文件的删除和移动权限，共享声明扩展修改权限并保持删除和移动权限归属于创建卡片。存量文件可以在人工规格确认、明确 AC 和影响验证下删除或移动。

### 验证

「验证」固定使用以下分组顺序：

```text
验证项 → 验证命令 → 覆盖率
```

#### 验证项

每条 AC 至少映射一个测试文件或验证命令：

```markdown
验证项：

- AC-01
  - 文件：tests/e2e/order/create_test.py
- AC-02
  - 命令：npm run validate-config
```

- AC 按编号升序排列
- `文件` 使用完整仓库相对路径，并位于「测试文件」边界内
- `命令` 与「验证命令」中的完整命令完全一致
- 同一测试文件可以验证当前卡片的多条 AC，并在全树中具有唯一归属卡片
- 测试文件必须由至少一条验证命令实际收集和执行

#### 验证命令

- 至少包含一项非交互、可重复、使用退出状态表示结果的命令
- 命令使用仓库声明并锁定的工具、包装器或项目脚本
- 命令从仓库根目录按列表顺序执行
- 命令自行准备和清理测试状态，敏感配置由批准的安全设施注入
- 命令可以执行测试、lint、静态分析、构建、结构检查、基准或其他可复核验证

新增行为和缺陷修复的 AC 必须先因目标行为缺失而有效失败，再在实现后通过。有效失败由目标行为导致；环境、收集或语法错误产生的结果属于无效证据。

保持既有行为的重构类 AC 使用实施前和实施后相同验证均通过的证据。结构、性能和配置目标使用对应的静态检查、基准或构建验证。禁止为了制造红灯故意破坏代码。红—绿或前后验证在实施时形成证据；永久记录保留最终结果，中间输出可以保存在临时验收产物中。

#### 覆盖率

覆盖率针对当前候选内容中归属当前卡片且仍然存在的可计量实现文件和共享文件。文件归属由「创建文件」中的当前路径确定；文件移动时以移动后的当前路径替换原记录，文件删除时移除对应记录。覆盖率计算直接使用当前所有权事实：

- 创建后又修改的文件继续计入
- 创建后移动的文件按更新后的当前路径计入
- 创建后删除的文件不再保留文件记录并位于覆盖率文件集之外
- 当前卡片覆盖率文件集由其「创建文件」中的当前实现文件和共享文件组成
- 「修改文件」中的 CDD 创建文件由其创建卡片计量；存量文件保持在 CDD 覆盖率归属之外
- 测试文件、生成代码和第三方代码位于覆盖率文件集之外

每个当前创建路径必须具有唯一创建卡片；存在重复创建事实或其他归属歧义时，覆盖率文件集无效并阻止卡片完成。当前候选内容和实现记录的一致事实构成覆盖率算法前提，Git 差异和可用的 Git 历史可以用于补充核对移动前归属。

有可计量代码时，「覆盖率」包含一个或多个机器可解析报告和适用阈值。报告格式由项目选择，并能够稳定映射完整文件路径和重复计算结果。

行覆盖率默认 `>= 90%`。技术栈能够可靠计量函数或方法时，函数覆盖率默认 `>= 80%`；计量能力缺失时写 `函数覆盖率不适用：<技术栈>；<具体原因>`，总数为零属于无效替代结果。存在多种技术栈时，逐项声明函数覆盖率不适用的技术栈。可计量代码集合为空时写 `覆盖率不适用：<具体原因>`。

提高默认阈值可以直接声明；降低默认阈值必须在「约束」中写明原因并由人明确批准。每份报告分别达到适用阈值，各技术栈独立判定。

> **示例（非规范性）**

```markdown
覆盖率：

- 报告：typescript；lcov；coverage/ORDER-01/lcov.info
- 行覆盖率：>= 90%
- 函数覆盖率：>= 80%
```

LCOV 在本节作为示例格式，项目可以使用其他机器可解析格式。

### 验收

「验收」至少包含一条 AC：

- 首行格式为 `- [ ] AC-两位编号 场景名` 或 `- [x] AC-两位编号 场景名`
- AC 从 `AC-01` 连续递增且不重复
- 第一项固定为 `Covers`，引用当前卡片的完整提供物或 `异常：EX-nn`
- `Covers` 后依次写可选 `Given`、恰好一个 `When` 和恰好一个 `Then`
- `Given` 和 `Then` 各包含一个或多个可断言列表项
- 公共前置条件可以使用位于第一条 AC 前的 `### Background`
- 参数化场景最后使用 `Examples` 表格

每个提供物和异常至少由一条 AC 有效覆盖。所有可验证要求必须出现在 AC 的场景或断言中；有效覆盖同时包含 `Covers` 引用和对应场景或断言。

`draft` 和 `ready` 卡片的 AC 全部为 `[ ]`。`doing` 卡片可以按有效验证进度勾选；`done` 卡片全部为 `[x]`。返工或修订时全部重置为 `[ ]`。

### 依赖

卡片 `depends` 使用按 ID 字典序排列的 YAML 块状列表。无依赖时省略，并省略「依赖」章节；存在依赖时，「依赖」章节按以下格式声明每条边使用的提供物：

```markdown
## 依赖

- INFRA-01
  - 能力：HTTP 路由装配
```

- 第一层逐项使用 `- <直接前置卡片ID>`，与 `depends` 目标一一对应并按 ID 字典序排列
- 第二层至少包含一项，逐项完整复制该目标卡片「提供」中的规范化标识，并按提供物类型和完整标识顺序排列
- 同一依赖使用保持唯一，并引用当前依赖目标的提供物
- 前置提供物名称变化时，全部直接依赖方同步更新引用并按影响验证判断兼容性

每次增删或调整 `depends` 或依赖使用后：

- 卡片修订为 `draft`
- 重新计算完整依赖图
- 核对每条边及其依赖使用对应当前卡片实际直接使用的提供物
- 重新执行 Card Lint 和 Card Review
- 再次完成规格确认

### 实现记录

「实现记录」中的「创建文件」和「修改文件」保存当前卡片仍然有效的所有权与影响事实；删除文件不保留文件记录，移动文件直接以当前路径替换原路径。Git 差异和历史承载创建、修改、删除与移动的事件顺序；其他分组保存当前轮次的有效事实。

分组顺序固定为：

```text
创建文件 → 修改文件 → 验证 → 决策 → 返工 → 修订 → 遗留
```

各分组按出现条件使用。所有文件记录使用指向文件的完整仓库相对路径。

> **示例（非规范性）**

```markdown
## 实现记录

创建文件：

- src/user/register/handler.py
- tests/e2e/user/register_test.py

修改文件：

- src/routes.py

验证：

- pytest tests/e2e/user/register_test.py --cov=src/user/register --cov-report=json:coverage/USER-01/coverage.json；通过
- 覆盖率（python）：行覆盖率 94.20%；函数覆盖率不适用：coverage.py 报告不提供函数或方法覆盖率

```

文件事实遵守以下规则：

- 当前卡片首次创建文件时写入「创建文件」
- 当前卡片后续修改自己创建的文件时仍保留在「创建文件」，每项所有权事实保持一处记录
- 当前卡片修改存量文件或允许修改的共享文件时写入「修改文件」
- 当前卡片删除文件时从「创建文件」或「修改文件」移除对应路径，不增加删除记录
- 当前卡片移动文件时以移动后的当前路径替换「创建文件」或「修改文件」中的原路径，不增加移动记录
- 同一分组中的记录保持唯一并按完整文本字典序排列
- 当前卡片中的同一路径只在一个文件分组出现；全树中的每个创建路径只有一个创建卡片，存在歧义时阻止完成
- 文件事实经可复核证据确认错误时直接修正，实现记录保留正确的当前事实
- 构建文件、缓存、覆盖率报告和其他临时验收产物保存在临时验收目录，其路径独立于文件记录

Git 差异和可用的 Git 历史保存文件事件并可以为移动前归属提供补充核对证据；实现记录只维护当前归属和影响判断需要的创建与修改事实。覆盖率与文件权限判断以当前候选内容和实现记录的一致事实为前提，提交作为可选的补充证据。

「验证」记录每条最终执行命令及 `；通过`，随后记录每份覆盖率的实际结果或不适用原因。结果失效时清除该分组，重新验证后写入最新事实。

「决策」分组由影响后续维护的关键实现取舍组成。每条决策同时满足：实施时存在两个以上符合规格的合理选择；当前选择会约束后续实现、兼容性、安全性或运维方式；该取舍需要在规格、名称、类型和代码结构之外补充维护语义。每条记录直接陈述当前选择、适用边界和维护约束。

规格、代码和配置已经直接表达的事实继续由各自权威位置维护；最终验证结果由「验证」分组维护；影响分析由来源操作的影响证据维护逐目标最终有效结论；验证过程、审查问题、命令尝试、修复顺序和中间候选由验证证据、临时验收产物与 Git 历史承载。完成候选中的「决策」由仍然有效且满足上述条件的条目组成。

「遗留」记录与规格和 AC 兼容的已知限制；必需工作全部完成是卡片完成的前提。

返工期间使用「返工」分组记录当前有效原因，修订期间使用「修订」分组记录当前有效原因；原因解决后的过程由 Git 历史承载，完成候选保留当前文件事实、最终验证结果、关键决策和适用遗留。

### 检查

#### Card Lint

至少检查以下确定性规则：

- YAML 字段、顺序、类型、日期、状态和 `confirmed_by`
- ID 格式、当前树唯一性、主题层级、卡片一级标题的展示名称和全部 CDD 结构机器引用
- 必选章节、条件章节、固定顺序、重复标题和空章节
- 提供物类型、顺序和全树唯一性
- 异常与 AC 编号、`Covers`、`Given`、`When`、`Then`、`Background` 和 `Examples` 结构
- `depends` 目标、排序、重复、自指和依赖图无环，以及「依赖」与每条边、目标提供物的一一对应
- 文件分组、路径格式、边界、创建路径唯一性和共享文件声明
- 状态保持的共享文件路径存在于创建卡片「实现记录」的「创建文件」
- 实现记录只使用完整路径，只有创建与修改文件分组且当前路径和归属关系有效
- 每条 AC 至少具有一个有效验证映射，文件和命令引用有效
- 覆盖率报告、适用阈值、不适用原因和路径有效
- README 不包含依赖或状态镜像

示例中的技术和值不参与 Card Lint。具体数据库、语言、框架和工具规则只在外部规范或卡片明确选择后检查。

#### Card Review

至少判断：

- 当前卡片和 README 链是否完整承载交付行为和边界
- 卡片是否局部完整、职责内聚并与兄弟节点正交
- 每条依赖使用是否对应当前卡片真实直接使用的前置提供物，并保持完整、最小充分
- 提供物、数据、运行和异常契约是否完整且各自具有唯一维护位置
- 文件边界是否最小充分，新增行为是否放在当前卡片创建的文件中
- 共享文件是否聚焦组合或全局配置职责，修改是否最小
- 存量文件修改、删除或移动是否必要并具有回归验证
- 所有可验证要求是否进入 AC，每条 AC 是否映射有效验证
- 覆盖率文件集边界和不适用原因是否成立
- AI 规格确认是否满足全部授权条件
- 高风险判定、人工确认和独立上下文检查要求是否正确
- 变更影响评估、生命周期状态和所需验证是否一致
- 影响候选集是否完整，逐目标实际影响或无影响结论是否具有对应作用路径或依据及证据，验证范围是否与实际影响一致且最小充分
- 状态保持的共享文件声明是否保持文件内容和路径、文件归属和验证依据，并对应创建卡片已经提供的共享基础能力和共享写入权限的人工确认
- 删除和影响验证范围是否完整，来源实例是否按来源操作正确聚合

#### 动态检查

卡片完成前至少检查：

- 每项实现、共享和测试源文件变更都位于当时有效的文件边界内并已正确记录，临时输出位于规定目录
- CDD 创建文件的修改、删除和移动符合创建卡片权限
- 验证项中的文件存在并被命令实际执行
- 新增行为和缺陷修复具有有效红—绿证据，其他目标具有适用的前后验证
- 全部适用验证命令具有与最新候选中的验证对象、环境和判据一致的通过证据
- 适用验证按当前候选和验证对象保持最小充分执行次数，复用证据与环境和判据保持一致
- 覆盖率文件集由最新候选内容和无歧义的所有权记录正确确定，并使用当前环境可用的补充证据核对，每份报告达到适用阈值
- 共享文件和存量文件变化已经完整收集影响候选集，逐目标实际影响或无影响结论具有证据，影响范围已经完成适用验证和处置
- 影响来源实例、影响触发事实、来源结果标识和逐目标消费关系与当前来源操作及集成事实一致
- 实现记录中的验证结果与实际输出一致
- 完成候选中的决策均为影响后续维护的当前有效取舍，实现与影响过程由验证证据、临时验收产物和 Git 历史承载

### 完成

编码 AI 在当前卡片保持 `doing` 时冻结规格上下文、集成基线标识、相关集成切片、交付路径集内容、初始卡片完成投影、卡片完成投影、来源累计差异、来源未接入余差、实现记录、验证证据、影响候选集、逐目标实际影响或无影响结论、影响范围和影响处置候选。完成门禁期间工作区卡片保持 `doing`，检查对象为这些冻结事实及卡片完成投影中的预期最终状态。完成条件是：

- 全部 AC 为 `[x]`
- 全部适用验证命令和覆盖率检查具有与当前冻结候选一致的通过证据
- 所需影响判定、验证和处置已经完成
- 当前卡片产生待影响验证来源实例时，处理后仍有活动来源的 `ready` 或 `done` 卡片已经形成待复核结论，该实例及其最终结果标识将在完成后保留到累计分类；当前来源实例是某个 `ready` 或 `done` 卡片的最后一个活动来源时，该卡片已经在累计影响投影视图中复验通过或形成完整影响处置候选；受影响的 `doing` 卡片已经暂停并确定串行顺序，该实例及其最终结果标识将在完成后保留到对应卡片消费
- Card Lint、动态检查、Card Review 和外部规范符合性检查通过
- 文件、实现、验证和实现记录与规格一致
- 实现记录已经归一化为当前文件事实、最终验证结果、关键决策和适用遗留
- 必需工作全部完成
- 高风险卡片的独立上下文检查问题清单为空

检查结果按生命周期中的变更影响评估条件处置。任一冻结事实或对应候选内容在门禁期间发生变化时，本轮门禁失效并从冻结步骤重新开始。

全部门禁通过后，再确认当前候选中的交付路径集内容与冻结内容完全一致，在同一完成操作中将冻结的来源未接入余差应用到最新集成基线，使来源相关结果达到冻结的卡片完成投影，并保留相关集成切片之外的最新事实。随后立即回读集成结果，逐项确认交付路径集中的规格、索引、实现、共享和测试文件内容，以及最终 YAML、状态、AC、实现记录、影响处置、来源实例状态、最终结果标识和逐目标关系与冻结投影一致并满足 Card Lint 与影响验证规则；检查通过后卡片完成投影整体生效。检查失败时从验证检查点共同恢复一致状态，修正后重新执行完成门禁。状态转换范围限于冻结投影。`done` 状态本身表示完成门禁已经通过，实现记录聚焦实现与验证事实。

### 提交

卡片完成后建议为该卡片创建对应 Git 提交，以便追踪规格、实现和验证结果。完成门禁与 Git 提交相互独立，提交授权由外部规范单独确定；验证检查点用于保护和验证候选内容。提交方式和集成策略由外部规范确定。
