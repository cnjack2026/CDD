# 卡片式开发

CDD（Card-Driven Development）是一种面向 AI 软件开发的规格方法，通过卡片组织边界明确、可实现且可验收的交付单元。项目以当前交付所需的最小设计开始编码，每轮工作保持有限上下文和可控智力负担。

## 设计哲学

### 规格

CDD 将需求、设计、开发任务和验收标准收敛为一张卡片，其职责如下：

- 定义当前交付拥有的能力和边界
- 给出足以产生一致外部行为的交付规格
- 指定正式代码和测试代码的修改范围
- 提供可执行测试、覆盖率阈值、修订条件和完成条件

卡片仅描述当前交付需要实现和验收的功能，每项能力由实际交付该能力的卡片维护。正文使用流畅、清晰、简洁的语句，正向说明应达到的行为、状态和边界。拒绝、失败、保密或禁止行为属于当前实现契约时，将其写入「规则」「约束」或「异常」，并明确触发条件和安全结果。

因此，一张卡片既是一份规格，也是一次开发任务。README 链、当前卡片和必要的直接前置卡片共同限定交付上下文；内部实现可以不同，外部行为必须与这些规格一致。

CDD 统一规定卡片的索引、依赖、状态、文件边界、验收证据和完成条件，使每项交付都有明确归属、固定结构、可执行判据和可复核的事实记录。

### 增量

项目或主题以当前交付所需的最小 README 链、主题边界和卡片为起点。后续卡片按交付顺序逐步建立或细化，当前卡片的交付上下文保持稳定。

每张转换为 `ready` 的卡片必须形成完整、可实现且可验收的规格，其完整性覆盖当前交付及其直接使用的既有契约。未来功能的规格在进入对应交付时创建或细化，并同步确定其内部设计。

实施中发现的新事实归入拥有对应能力的卡片。保持当前规格的新事实作为内部实现选择处理；改变当前或既有契约的新事实按修订和影响验证规则处理。

全树随交付持续扩展，单次编码始终围绕一个局部完整的交付单元进行。

### 术语

下表只收录需要跨章节消歧的核心术语。格式字段和局部名称在其所属章节定义，并由该章节唯一维护。

| 术语       | 定义                                                                                            |
| ---------- | ----------------------------------------------------------------------------------------------- |
| 卡片根目录 | 卡片树的 `docs/` 目录，由 `docs/README.md` 维护                                                 |
| 主题目录   | 卡片树中包含一个 `README.md` 和至少一个直属节点的目录                                           |
| 节点       | 目录节点和卡片的统称                                                                            |
| 目录节点   | 卡片根目录和主题目录的统称                                                                      |
| 卡片       | 主题目录中的主题文件，也是最小交付单元                                                          |
| 直属节点   | 当前目录节点直接索引的主题目录或卡片                                                            |
| 直接前置   | 其能力被当前节点直接使用，或当前节点开工前必须等待其完成的节点                                  |
| 提供物     | 卡片声明并供依赖方使用的输出契约                                                                |
| 文件边界   | 当前卡片在「文件」章节声明的目录和完整文件路径形成的实现写入范围                                |
| 重叠文件   | 完整路径同时位于多张卡片文件边界内的正式代码文件                                                |
| 文件记录   | 「实现记录」中累计保存的当前卡片创建、修改、删除和移动实现文件的事实                            |
| 外部规范   | 已确认的用户要求与当前工作范围适用的仓库指令文件                                                |
| 规格确认   | 人将卡片从 `draft` 转换为 `ready` 的状态转换                                                    |
| 完成门禁   | 卡片从 `doing` 转换为 `done` 前由编码 AI 执行的测试、检查、复核和一致性判定                     |
| 独立审核   | 编码 AI 基于最新工作树重新核对全部语义判据，并单独形成当轮结论的完整审核                        |
| 卡片轮次   | 首轮从卡片创建开始，后续轮次从卡片由 `done` 转换为 `draft` 开始，每轮在卡片转换为 `done` 时结束 |
| 完成检查   | 目录节点从 `doing` 自动转换为 `done` 前执行的 Card Lint、Card Review 和规范符合性检查           |
| 删除       | 从当前工作树中原子移除目标节点，并同步移除删除集内卡片的实现、规格声明、依赖和索引事实          |
| 影响验证   | 删除、前置修订或重叠文件变更时，确定受影响卡片并复验其中保留的已完成卡片                        |

全文固定使用上述核心术语。卡片或目录节点达到 `done` 写作“完成”，卡片回到 `draft` 写作“修订”，目录节点转换为 `done` 前的检查写作“完成检查”。

### 卡片树

卡片按主题目录组织为一棵树，默认卡片根目录是 `docs/`：

```text
docs/
├── README.md
├── 基础设施/
│   ├── README.md
│   └── 仓库脚手架.md
├── 用户/
│   ├── README.md
│   ├── 手机号注册.md
│   └── 账户/
│       ├── README.md
│       └── 换绑手机号.md
└── 订单/
    ├── README.md
    └── 创建订单.md
```

卡片根目录的直属节点均为主题目录。

每个主题目录恰有一个 `README.md`，并至少包含一个直属节点。直属节点可以是卡片，也可以是子主题目录。主题目录可以逐层嵌套，每一层都表达清晰且正交的功能归属。

目录位置表达功能归属，节点 ID 表达稳定身份，两者各司其职：

- 主题目录名与卡片的无后缀文件名统称节点名称。节点名称使用简短的功能名称，至少包含一个中文字符，并且只由中文字符、英文字母和阿拉伯数字组成
- 主题目录索引固定使用 `README.md`
- 一级标题和 README 章节使用展示名称。展示名称只在节点名称的中文字符与英文字母或数字的边界处加入一个半角空格；移除该空格后必须与节点名称完全一致
- 主题目录和卡片使用节点 ID；节点 ID 独立于节点名称，重命名时保持不变
- 同一父主题目录下的卡片和子主题目录保持职责正交

需求、设计、约束、配置、测试、验收和实现记录统一写入所属卡片。各级 README 描述当前项目或主题目录的整体功能，并索引直属节点。

这种结构支持项目沿卡片树的宽度和深度扩展，同时使索引和卡片规模保持可控。

### 上下文

#### README 链

README 链是从卡片根目录到当前卡片所在主题目录的有序 README 序列。当前卡片的规格上下文按以下顺序装载：

```text
README 链
→ README 链中 `depends` 指向的直接前置主题目录 README
→ 当前卡片
→ 仅在需要契约细节时，装载提供该契约的直接前置卡片
```

README 提供功能、归属和主题依赖，当前卡片提供交付规格，直接前置卡片只补充当前实现需要的契约细节。前置卡片的内部实现不属于当前卡片的规格上下文。

当前卡片使用主题依赖中的部分能力时，目标契约由直接前置主题目录的 README 索引路径和对应卡片共同确定。传递前置只参与依赖图计算，不扩展当前卡片的规格正文。

编码 AI 的活动上下文限定为外部规范、README 链、当前卡片、当前实现文件，以及当前实现确实需要的直接前置契约。

读取直接前置卡片时装载定义所用提供物的相关章节；测试、实现记录和内部实现按影响验证或兼容性判断的实际需要装载。

查重、删除和影响范围计算通过全树索引处理，活动上下文只保留当前交付相关的叙事正文。

一致实现当前卡片需要更多事实时，返回设计阶段修订卡片、依赖或主题边界，将所需事实纳入局部完整的交付上下文后继续。

#### 全树索引

全树索引是从全部 README 和卡片中提取机器事实形成的视图，不加载叙事正文。

该索引包含节点类型、路径、节点 ID（卡片根目录除外）、`status`、`depends`，以及卡片「提供」章节、「数据」章节中的数据库表结构和函数定义、「文件」章节和「测试」章节中「测试文件」分组的固定格式条目。

全树索引用于查重、检查依赖图、核对数据库表的唯一归属和外键目标、核对数据库函数定义、核对文件边界和测试文件的 AC 归属、核对开工条件，以及计算删除范围、修订影响和影响验证范围。

全树索引是 AI 按当前工作需要从卡片树构造的即时逻辑检查视图。AI 使用现有读取、搜索、推演和临时计算能力形成该视图，并在每次使用时以当前卡片树刷新全部机器事实。

#### 外部规范

已确认的用户要求和适用于当前工作范围的仓库指令文件（包括适用的 `AGENTS.md`）共同构成当前交付的外部规范。写卡和实现开始前必须读取外部规范，卡片完成前必须重新核对其中仍然有效的要求。

卡片及其交付内容必须符合外部规范。存在冲突或信息缺口时，`draft` 卡片保持 `draft`，其他状态的卡片修订为 `draft`。

### 依赖

`depends` 是统一的依赖声明，方向恒定为“依赖节点 → 直接前置节点”：

- 每条边表示当前节点直接使用前置节点的能力，或必须等待前置节点完成后才能开工
- `depends` 只写直接前置，传递前置由递归遍历直接边得出
- 卡片依赖连接同一主题目录内的卡片
- 主题依赖连接同一父目录节点下的兄弟主题目录
- 直接前置位于其他主题目录时，按照「README / 依赖」的跨主题依赖提升规则声明主题依赖
- 正式代码的完整文件路径可以由多张卡片声明或记录，`depends` 仍按直接前置关系确定；重叠文件变更按影响验证规则处理

卡片依赖图和主题依赖图都必须是有向无环图。开工条件所需的前置状态和修订影响均从直接边出发递归计算。

主题依赖以完整主题目录为单位。前置主题目录整体聚合为 `done` 后，依赖方子树才能开工。

主题依赖的目标与建立条件由「README / 依赖」统一规定。

已完成的主题目录发生变化，导致原完成检查结果失效时，按 README 状态同步规则更新该主题目录，并从主题依赖的反向索引出发，将影响传播至所有直接和传递依赖方。

每个受影响的依赖主题目录及其祖先都必须重新执行完成检查；其子树中的卡片按「设计哲学 / 生命周期 / 前置修订」和「设计哲学 / 生命周期 / 影响验证」处理。全部直接前置主题目录重新成为 `done` 后，受影响的依赖主题目录才能再次完成。

### 执行

#### 职责

- 人提供需求和项目事实，回答领域问题，执行规格确认，并抽查验收结果
- 写卡 AI 确定功能归属，完成查重和澄清，声明直接依赖与文件边界，并执行 Card Lint 和 Card Review
- 编码 AI 核对开工条件，按 AC 实施验收测试驱动开发（ATDD），在文件边界内完成实现、记录和验证，并执行完成门禁

技术选择依次遵循当前交付的外部规范、当前卡片、必要的直接前置卡片、仓库配置和现有工程约定。尚未明确且会改变外部行为、兼容性或卡片间契约的选择必须写入当前卡片；只影响内部实现的关键选择写入「实现记录」。

Card Lint、动态检查、Card Review、规范符合性检查和完成检查都是 AI 必须形成可复核结论的检查职责。AI 使用当前环境已有能力，依据实际文档、代码、命令输出和运行结果完成检查，并以判据和证据决定检查结果。

卡片将专用检查程序声明为提供物时，该程序成为项目交付内容。

#### 交付

一次卡片交付按以下顺序执行：

1. 读取外部规范和规格上下文，通过全树索引完成查重，并核对节点 ID、依赖、提供物、文件边界和测试文件归属
2. 创建或修订 `draft` 卡片，通过 Card Lint 和 Card Review 后由人执行规格确认，使卡片转换为 `ready`
3. 开工条件成立后先将卡片转换为 `doing`，再产生实现文件变更
4. 按 AC 建立初始失败的验收测试，再完成对应实现；测试进度、实现文件变更和关键决策同步记录在卡片中
5. 执行完成门禁中的验证：运行卡片声明的测试命令和覆盖率检查，对实际变更的重叠文件执行影响验证，执行 Card Lint 和动态检查，再由编码 AI 在同一最新工作树上连续执行三轮独立审核
6. 规格问题按修订规则处理；全部完成条件成立时，完成门禁通过。门禁通过后将卡片转换为 `done`，按 README 状态同步规则更新各级状态，再对已完成的前置修订执行影响验证

各步骤中的格式、状态、记录和检查判据由对应章节唯一规定，本节只规定执行顺序。

#### 并行

并行卡片使用相互隔离的工作区，同一时刻每张卡片只由一个编码 AI 产生变更。重叠文件变更按顺序逐一集成，前一张卡片的集成结果作为下一张卡片的最新集成基线。每张卡片都在自己的最新集成基线上执行影响验证；测试资源按「卡片 / 测试 / 并行」隔离。

### 生命周期

#### 状态

卡片状态机固定为：

```text
draft ──规格确认──▶ ready ──开工──▶ doing ──完成门禁通过──▶ done
  ▲                  │              │                          │
  └──────────────────┴────修订──────┴──────────────────────────┘
```

- `draft`：卡片正在编写或修订
- `ready`：卡片已经确认，开工条件满足后可以开工
- `doing`：卡片工作正在实施
- `done`：卡片工作已经完成

只有 `ready` 卡片可以转换为 `doing`。开工条件是当前卡片为 `ready`，并且从当前卡片和 README 链中各主题目录的直接 `depends` 出发，递归可达的全部前置都为 `done`。

卡片修订为 `draft` 后，必须重新经过规格确认才能转换为 `ready`。`doing` 卡片执行「卡片 / 完成」规定的完成门禁，并在门禁通过后转换为 `done`。

#### 修订

已确认的规格、文件边界、元数据、实施前提、实现结果或完成证据需要重新确认时，修订能力所属的原卡片。从 `ready`、`doing` 或 `done` 转换为 `draft` 统一记为修订；仅由状态同步产生的 `status` 和 `updated` 变化不会开始新的卡片轮次。

以下任一情况触发修订：

- 规格存在歧义、矛盾或缺失
- 已确认的需求、约束、卡片规格、文件边界或元数据发生变化
- 当前实现需要改变其他卡片的提供物
- `doing` 卡片开工条件涉及的任一前置不再为 `done`
- 前置修订实际影响当前卡片规格
- `done` 卡片的实现或完成证据经抽查不符合当前规格

修订 `ready`、`doing` 或 `done` 卡片后，卡片转换为 `draft`，全部 AC 重置为 `[ ]`，删除需要重新验证的测试通过和覆盖率结论，并在「实现记录」中写明可复核的修订原因。修订 `draft` 卡片时保持 `draft`，全部 AC 保持 `[ ]`。

`doing` 卡片的规格和实施前提仍然有效时，普通实现错误或测试失败属于当前轮次的未完成工作，卡片保持 `doing` 并继续修复。发现 `done` 卡片存在缺陷时，修订能力所属的原卡片；需要新增独立提供物时，新建卡片。

#### 删除

删除独立于卡片状态机。任意状态的卡片或主题目录都可以删除；删除主题目录时，其整棵子树属于同一删除集。

删除后的当前工作树和卡片树必须同时满足：

- 删除集中的卡片、目录、「实现记录」、README 索引和依赖边不再存在
- 不存在空主题目录；失去最后一个直属节点的主题目录属于删除集
- 仅供删除集内卡片使用的正式代码、测试代码和配置均不再存在，其提供物声明及对应实现、制品声明及对应文件也不再存在；其他卡片当前规格仍需的文件和内容继续保留
- 文档、代码、测试和配置不再引用删除集中的节点及其产生的事实
- 原直接或传递依赖删除目标的节点已纳入删除集，或已修订为不再依赖该目标并保持自洽
- 全树索引、文件边界、依赖图、README 聚合状态和保留节点的检查结果与删除后的当前事实一致

删除集中的 ID 在删除后不再占用；新增节点仍按其所属章节的 ID 生成和唯一性规则分配。

#### 前置修订

前置修订的影响范围由直接边的反向索引递归确定：

- 开工条件不再成立的 `doing` 卡片修订为 `draft`
- 尚未开工的卡片在开工条件重新满足前保持阻塞
- 规格确实受到影响的 `draft` 卡片保持 `draft`，`ready` 卡片修订为 `draft`
- `done` 依赖方纳入影响验证；契约兼容且影响验证通过时保持 `done`，其余情况修订为 `draft`

#### 影响验证

执行删除、前置修订重新完成或重叠文件变更时执行影响验证。删除和前置修订使用变更前的全树索引，重叠文件变更使用执行影响验证时的最新全树索引；影响范围按以下规则合并并去重：

- 删除从删除集中的节点及其祖先目录节点出发，沿卡片依赖和主题依赖的反向索引递归收集直接和传递依赖方，再排除删除集中的节点
- 前置修订从修订节点及其祖先中完成结果失效的目录节点出发，沿卡片依赖和主题依赖的反向索引递归收集直接和传递依赖方；主题依赖命中的主题目录包含其子树中的全部卡片
- 重叠文件变更先收集文件边界包含每个实际变更路径的其他卡片，再从这些卡片及其 README 链出发，沿卡片依赖和主题依赖的反向索引递归收集直接和传递依赖方

只有受影响范围内保留的 `done` 卡片需要复验。复验按照前置到依赖方的拓扑顺序执行每张卡片声明的全部测试命令和覆盖率检查，并通过 Card Review 确认相关契约保持兼容。

完成删除还要求删除范围完整，并确保影响范围内保留的 `done` 卡片契约自洽，且其全部测试命令和覆盖率检查通过。任一条件不成立，删除都未完成。

前置修订的影响验证在修订节点及其全部前置重新成为 `done` 后执行。受影响的 `done` 卡片在契约兼容且复验通过时保持 `done`；契约不兼容或复验失败时修订为 `draft`。

重叠文件变更在最新集成基线上执行影响验证。受影响的 `done` 卡片全部复验通过后，当前卡片才能完成；复验失败时，当前卡片保持 `doing`。

已确认的新行为需要改变受影响卡片的规格时，对应卡片按修订规则处理。在这些卡片重新完成并基于同一最新集成基线复验通过前，当前卡片保持 `doing`。

#### 规范符合性

规范符合性检查确认以下内容：

- 本次完成范围内的文档、代码、测试和配置符合当前工作范围适用的仓库指令文件规定的执行要求
- 本次完成范围内的文档、代码、测试和配置完整满足仍然有效的用户要求

## README

README 用于说明目录节点并索引其直属节点。YAML 描述当前目录节点，一级标题标识当前项目或主题目录，整体功能描述界定当前目录节点的功能定位与整体边界，后续章节分别描述各直属节点。

父 README 中的主题目录章节是该主题目录面向上层的简明投影，用于索引和依赖判断。主题目录的整体功能发生变化时，自身 README 与父 README 中的投影保持语义一致。

### 格式

README 使用封闭格式。本节统一规定字段、标题、顺序、出现条件和正文结构。每项内容都归入唯一对应的既定结构；既定结构不足以承载内容时，调整职责边界或拆分目录。

以下示例是对应结构的规范模板。实际 README 沿用相同结构，并将示例内容替换为当前已确认的事实。

#### 共用规则

两类 README 共用以下约束：

- YAML 头必须位于文件开头且恰有一段；必填字段按各自格式规定的顺序排列，`depends` 始终位于最后
- 字段集合与对应目录节点格式完全一致，所有标量值和 `depends` 列表项使用双引号字符串
- `status` 的取值范围是 `draft`、`ready`、`doing`、`done`
- `created`、`updated` 使用有效的 `YYYY-MM-DD`，并满足 `updated >= created`
- `created` 写入后不变，内容或状态变化时刷新 `updated`

两类目录节点使用相同的 README 正文格式，只有 YAML 字段不同。

#### 卡片根目录

卡片根目录 `README.md` 的完整字段集合和顺序为 `project`、`status`、`created`、`updated`。`project` 填写非空项目名称，规范模板如下：

```markdown
---
project: "demo"
status: "ready"
created: "2026-07-21"
updated: "2026-07-21"
---

# demo

demo 提供用户身份与订单管理能力。

## 用户

- ID: USER
- STATUS: ready

提供用户身份建立与手机号维护能力，包括手机号注册和已登录用户换绑手机号。

## 订单

- ID: ORDER
- STATUS: ready

提供订单创建能力，接收用户提交的商品与收货信息，并生成和保存可追踪的订单。
```

#### 主题目录

主题目录 `README.md` 的必填字段和顺序固定为 `id`、`status`、`created`、`updated`；存在直接主题依赖时才在最后增加 `depends`。规范模板如下：

```markdown
---
id: "ORDER"
status: "ready"
created: "2026-07-21"
updated: "2026-07-21"
depends:
  - "USER"
---

# 订单

提供订单创建及后续订单生命周期管理能力。

## 创建订单

- ID: ORDER-01
- STATUS: ready

提供订单创建能力，根据用户提交的商品与收货信息生成并保存订单。
```

主题目录 ID 的每个片段必须匹配 `[A-Z][A-Z0-9]*`。顶层主题目录使用单个片段，子主题目录 ID 为“父主题目录 ID + `-` + 本级片段”。主题目录 ID 片段以字母开头，与卡片末尾序号保持清晰区分。

主题目录名遵循卡片树命名规则。在同一父目录节点下重命名主题目录时，ID 保持不变，并同步目录名、自身 README 的一级标题和父 README 的章节名称。

删除主题目录按「设计哲学 / 生命周期 / 删除」执行。更换主题目录的父目录节点前，整棵子树中的卡片都要处于 `draft` 且没有「实现记录」；迁移时重新编排子树 ID、依赖引用和 README 索引。

### 正文

YAML 头之后依次写一级标题、整体功能描述和零个或多个直属节点章节，顺序固定，各部分之间恰有一个空行。一级标题和整体功能描述始终必填。

主题目录至少包含一个直属节点章节；尚未建立主题目录时，卡片根目录 README 可以没有直属节点章节。卡片根目录 README 的一级标题与 `project` 完全一致；主题目录 README 的一级标题使用当前目录的展示名称。

整体功能描述使用恰好一个叙事段落，说明当前项目或主题目录提供什么功能及其整体边界。该段落只承载功能信息；实现方式和开发过程写入卡片，依赖关系写入 `depends`。

每个直属主题目录或卡片恰占一个 `##` 章节。主题目录章节和卡片章节均使用对应节点的展示名称，并与对应节点的一级标题一致。同一父目录节点下的章节名和直属节点名称分别保持唯一。

每个直属节点章节固定由以下三部分组成，全部必填且顺序固定：

1. `- ID: <直属节点ID>`
2. `- STATUS: <直属节点状态>`
3. 功能描述正文

章节标题后空一行，`ID` 和 `STATUS` 各占一个非空列表项并连续书写，之后空一行直接写功能描述：

- `ID` 必须等于直属卡片 YAML 的 `id` 或直属主题目录 README YAML 的 `id`
- `STATUS` 必须等于对应直属节点的当前状态
- 功能描述聚焦对外做什么；实现方式和开发过程写入卡片，依赖关系写入 `depends`
- 功能描述使用恰好一个叙事段落
- 卡片描述必须概括当前卡片的全部提供物及其作用，主题目录描述必须聚合概括整个子树

功能描述应使依赖方通常无需打开下级卡片，便能了解节点提供的能力。需要查看精确契约、规则或边界时，再通过 ID 和章节名称定位对应卡片。

章节顺序表示人工设定的优先级；依赖和开工顺序由 `depends` 与状态共同决定。

### 状态

卡片状态的权威位置由「卡片 / 元数据」定义。父 README 中对应卡片章节的 `STATUS` 必须与其一致。

#### 聚合

目录节点 YAML 中的 `status` 由直属节点状态、直接前置主题目录状态和当前目录节点的完成检查结果共同确定，并按以下顺序聚合：

1. 直属节点非空、全部为 `done`，全部直接前置主题目录为 `done`，并且当前目录节点的 Card Lint、Card Review 和规范符合性检查通过时，聚合为 `done`
2. 至少一个直属节点为 `doing` 或 `done` 时，聚合为 `doing`
3. 没有 `doing` 和 `done`，且至少一个直属节点为 `ready` 时，聚合为 `ready`
4. 其他情况聚合为 `draft`

直属节点和全部直接前置主题目录均为 `done` 时，自动执行当前目录节点的完成检查。检查通过后，当前目录节点从 `doing` 转换为 `done`；检查未通过时，当前目录节点保持 `doing`，修正问题后重新检查。

嵌套主题目录按照从叶节点到卡片根目录的顺序逐层检查和聚合，父目录节点等待全部直属节点完成。

卡片根目录没有直属主题目录时聚合为 `draft`。

父 README 中对应主题目录章节的 `STATUS` 必须与主题目录 YAML 一致。

#### 同步

卡片或目录节点状态变化时，按以下顺序同步：

1. 更新状态权威位置及其 `updated`
2. 存在直属父 README 时，更新其中对应章节的 `STATUS`
3. 根据直属节点状态重新聚合父目录节点；完成条件成立时，执行完成检查
4. 父目录节点状态变化时，更新其父 README 的 `STATUS` 并继续向上同步，直至卡片根目录
5. 刷新每个实际修改的 README 的 `updated`
6. 对状态权威位置和实际修改的 README 执行 Card Lint

同步完成后，状态权威位置、直属父 README 对应章节中的 `STATUS`、全部祖先目录节点的聚合状态及其上级投影必须一致。

只有同步结果通过 Card Lint，规格确认、开工或卡片完成转换才成立。目录节点未通过完成检查时保持 `doing`；发生修订或完成检查结果失效后，节点按同步规则保持对应的非 `done` 状态。

新增、删除、移动、重命名或修订后代节点，修改 README 内容，或者增删或改向 `depends` 时，直接受影响的目录节点及其全部祖先原有的完成检查结果均失效，随后按上述规则重新聚合。

已完成主题目录新增卡片后，同时存在已完成和未完成的直属节点，因此该主题目录及其祖先聚合为 `doing`。新增卡片及其规格仍按卡片状态机从 `draft` 开始。

节点索引信息变化时，先同步直属父 README 中的对应章节，再核对该 README 的整体功能描述。变化影响上层主题目录时，逐层同步主题目录投影和整体功能描述，直至卡片根目录。主题目录重命名时，其 README 一级标题必须与新名称一致。

### 依赖

主题目录的 `depends` 声明当前主题目录的直接前置主题目录，目标为同一父目录节点下的兄弟主题目录 ID：

- 有依赖时使用按 ID 字典序排列的 YAML 块状列表
- 无依赖时省略 `depends`
- 依赖目标唯一且不指向当前主题目录，主题依赖图保持有向无环
- 列表顺序不表示优先级

直接前置位于其他主题目录时，将依赖提升到双方最低公共祖先下的第一层兄弟主题目录。例如，“订单”主题目录需要“用户”主题目录的全部能力时，其直接前置是“用户”主题目录，`depends` 写入“订单”主题目录 README。

只需要部分能力时，先调整主题目录边界，使这部分能力形成完整且最小的主题目录。

若 A → B 且 B → C，A 通过 B 间接获得 C，则 A 只需保留指向 B 的直接边。A 本身也直接使用 C 时，可以同时声明 A → C。每次增删或改向依赖后，都必须重新计算完整的主题依赖图。

第三方库、外部服务和环境变量写入卡片的「约束」「数据」或「运行」，`depends` 专门承载卡片树内的直接前置节点。

增删或改向主题目录的 `depends` 后，按以下规则重新核对当前主题目录子树：

- 未完成卡片重新计算开工条件；不再满足开工条件的 `doing` 卡片按修订规则处理
- `done` 卡片按「设计哲学 / 生命周期 / 前置修订」处理
- 已完成主题目录及其祖先的完成检查结果按 README 状态同步规则失效；全部直接前置主题目录重新成为 `done` 后，当前主题目录才能再次自动完成

### 检查

#### Card Lint

至少检查以下确定性规则：

- 树节点类型、空主题目录、YAML 头数量、字段集合、字段顺序、值类型、日期和状态
- 主题目录 ID、父子关系、全库唯一性和目录命名
- 一级标题与卡片根目录的 `project` 或主题目录的展示名称一致，整体功能描述存在且为恰好一个叙事段落
- 直属章节与实际直属节点一一对应，章节名唯一，展示名称符合空格规则并匹配目录名或无后缀文件名
- 每个直属节点章节的 `ID`、`STATUS` 和功能描述完整且顺序正确，功能描述为恰好一个叙事段落
- 目录节点的聚合状态、完成检查前提和 README 状态同步结果正确
- `depends` 目标、层级、排序、重复、自指和主题依赖图的有向无环性

#### Card Review

至少判断以下需要规格语义判断的内容：

- 当前目录节点及其 README 链是否完整承载外部规范规定的交付行为和边界
- 整体功能描述是否准确说明当前目录节点的功能定位与整体边界
- 主题目录整体功能描述是否与父 README 中的主题目录章节保持语义一致
- 直属节点功能描述是否足以说明提供什么，是否遗漏、虚构或改变能力
- 兄弟主题目录、直属卡片与子主题目录是否职责正交，是否存在职责重叠
- 主题依赖是否为完整且最小的真实直接前置，是否需要让依赖方等待整个前置主题目录完成
- 主题目录拆分是否具有清晰的交付含义，并让依赖方保持最小上下文

## 卡片

### 格式

卡片使用封闭格式。本节统一规定字段、标题、顺序、出现条件和正文结构。每项内容都归入唯一对应的既定结构；既定结构不足以承载内容时，调整职责边界或拆分卡片。

以下规范模板展示一张完整的 `draft` 卡片。实际卡片沿用相同结构，以当前已确认的事实替换示例内容，并仅保留满足出现条件的章节：

````markdown
---
id: "USER-01"
status: "draft"
created: "2026-07-21"
updated: "2026-07-21"
---

# USER-01：手机号注册

## 提供

- 接口：POST /api/register
- 表：users

## 目标

新用户使用大陆手机号和短信验证码开户，无需设置密码。

## 规则

- 手机号在判重和存储前统一转换为 E.164 格式
- 同一标准化手机号最多对应一个用户账户

## 约束

代码目录树：

```text
src/user/register/
├── index.ts
├── migrations/
│   └── 001_create_users.sql
├── normalize.ts
└── tests/
    ├── migration-schema.test.ts
    ├── register-success.test.ts
    ├── reject-duplicate.test.ts
    └── reject-invalid-credentials.test.ts
```

运行环境版本：

| 环境       | 版本      |
| ---------- | --------- |
| PostgreSQL | `>= 18.0` |

## 数据

- 接口：POST /api/register
  - 认证：免登录
  - 入参：phone（大陆手机号）、code（6 位数字，5 分钟有效）
  - 成功：201 { token, userId }
- 表：users
  - COMMENT：用户账号
  - 字段：
    - user_id：UUID NOT NULL DEFAULT uuidv7()；COMMENT '用户标识'
    - phone：TEXT NOT NULL；COMMENT 'E.164 格式手机号'
    - created_at：TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP；COMMENT '创建时间'
  - 主键：user_id
  - 唯一：phone

## 异常

- EX-01：手机号已注册时返回 409，显示“该手机号已注册”
- EX-02：手机号不是有效的大陆手机号时返回 400，显示“请输入有效的大陆手机号”
- EX-03：短信验证码格式错误、不匹配或已过期时返回 400，显示“短信验证码无效”

## 文件

正式代码：

- src/routes/index.ts
- src/user/register/

测试代码：

- src/user/register/tests/

## 测试

测试文件：

- AC-01
  - src/user/register/tests/migration-schema.test.ts
  - src/user/register/tests/register-success.test.ts
- AC-02
  - src/user/register/tests/reject-duplicate.test.ts
- AC-03
  - src/user/register/tests/reject-invalid-credentials.test.ts

测试命令：

- bun test src/user/register/tests --coverage --coverage-reporter=lcov --coverage-dir=coverage/USER-01

覆盖率：

- 报告：javascript-typescript；lcov；coverage/USER-01/lcov.info
- 行覆盖率：>= 90%
- 函数覆盖率：>= 80%

## 验收

- [ ] AC-01 正常注册并标准化手机号
  - Covers
    - 接口：POST /api/register
    - 表：users
  - Given
    - 规格上下文提供的后端原生迁移命令可用，并已准备一个空 PostgreSQL 数据库和当前有效验证码 `123456`
  - When 使用后端原生迁移命令执行一次迁移，向 `users` 插入 `phone=+8613900139000` 的既有记录，再次执行同一命令，然后调用 `POST /api/register`，`phone=13800138000`、`code=123456`
  - Then
    - 两次迁移均成功，第二次执行后 `schema` 不变，既有记录的全部字段值不变
    - PostgreSQL catalog 中 `users` 的字段顺序、类型、空值规则、默认值、主键、唯一约束以及表和字段的中文 `COMMENT` 与「数据」章节一致
    - 返回 `201`、`token` 与 `userId`
    - `users` 表新增一条包含数据库生成的 UUID v7 `user_id`、`phone=+8613800138000` 和数据库自动设置的 `created_at` 的记录
    - 未提供身份凭证也可完成注册
    - 注册过程不接收或存储密码
    - 正式代码和测试代码分别落在声明的范围内，目录结构与代码目录树一致
- [ ] AC-02 按标准化手机号拒绝重复注册
  - Covers
    - 异常：EX-01
  - Given
    - `users` 表已有 `phone=+8613800138000` 的记录
  - When 调用 `POST /api/register`，`phone=13800138000`、`code=123456`
  - Then
    - 返回 `409`，显示“该手机号已注册”，`users` 表记录数不变
- [ ] AC-03 拒绝无效注册凭据
  - Covers
    - 异常：EX-02
    - 异常：EX-03
  - Given
    - 手机号 `<phone>` 未注册
    - 验证码 `<code>` 的状态为 `<codeStatus>`
  - When 调用 `POST /api/register`，`phone=<phone>`、`code=<code>`
  - Then
    - 返回 `400`，显示“<message>”，`users` 表记录数不变
  - Examples

    | phone       | code   | codeStatus | message                |
    | ----------- | ------ | ---------- | ---------------------- |
    | 12345       | 123456 | 有效       | 请输入有效的大陆手机号 |
    | 13800138000 | 12345  | 格式错误   | 短信验证码无效         |
    | 13800138000 | 654321 | 不匹配     | 短信验证码无效         |
    | 13800138000 | 123456 | 已过期     | 短信验证码无效         |
````

### 元数据

卡片 YAML 字段集合固定如下：

| 必填字段                             | 可选字段  |
| ------------------------------------ | --------- |
| `id`、`status`、`created`、`updated` | `depends` |

- YAML 头必须位于文件开头且恰有一段，必填字段顺序固定为 `id`、`status`、`created`、`updated`；存在直接卡片依赖时才在最后增加 `depends`
- 所有标量值和 `depends` 列表项使用双引号字符串；日期和状态规则与 README 相同
- 卡片 ID 为“完整主题目录 ID + `-` + 两位序号”，序号范围为 `01`–`99`
- 同一主题目录的直属卡片序号保持唯一；新增卡片使用 `01`–`99` 中当前未占用的最小序号，没有可用序号时先拆分子主题目录
- 卡片 ID 在当前卡片树中保持全库唯一，用于标识逻辑归属
- 卡片的文件名固定为“节点名称 + `.md`”

YAML 头、一级标题和第一个二级章节之间各恰有一个空行。一级标题恰好一个，格式为 `# <ID>：<展示名称>`。ID 与 YAML 完全一致；展示名称由卡片无后缀文件名生成，并与父 README 的对应章节名称一致。一级标题后的全部内容均归入二级章节。

在同一父主题目录内重命名卡片时，ID 保持不变，并同步修改文件名、一级标题和父 README 中的章节名称。删除卡片按「设计哲学 / 生命周期 / 删除」执行。更换卡片的父主题目录前，卡片必须处于 `draft` 且没有「实现记录」；迁移时重新编排 ID、依赖引用和 README 索引。

卡片 YAML `status` 是卡片状态的唯一权威；父 README 只镜像卡片 ID、状态和功能描述。

### 正文

#### 通用规则

卡片二级章节的固定顺序是：

```text
提供 → 目标 → 术语 → 规则 → 约束 → 交互 → 界面 → 状态
→ 数据 → 运行 → 异常 → 文件 → 测试 → 验收 → 实现记录
```

每种二级章节至多出现一次，二级标题前后各恰有一个空行，全部正文归入这些章节。三级标题只有「验收」内的 `### Background` 一种。

| 章节       | 出现条件                     | 固定结构                                  | 内容边界                                                                   |
| ---------- | ---------------------------- | ----------------------------------------- | -------------------------------------------------------------------------- |
| `提供`     | 必选                         | 规范化提供物列表                          | 当前卡片拥有的输出契约，至少一项                                           |
| `目标`     | 必选                         | 一个叙事段落                              | 当前交付需要实现的结果与适用范围                                           |
| `术语`     | 有领域词歧义时               | `- <术语>：<定义>` 列表                   | 当前卡片需要消除歧义的领域名词                                             |
| `规则`     | 有业务规则时                 | 一层无序列表                              | 计算、判定、资格、优先级、幂等和并发等业务规则                             |
| `约束`     | 有硬指标或声明正式代码目录时 | 固定命名分组                              | 代码结构，以及性能、安全、隐私、合规、兼容、可用性和依赖版本等硬指标       |
| `交互`     | 有正常操作路径时             | 一层有序列表                              | 用户或系统的正常操作步骤                                                   |
| `界面`     | 有界面要求时                 | 一层无序列表                              | 布局、视觉、文案、界面状态、无障碍和本地化要求                             |
| `状态`     | 有业务实体状态转换时         | `当前状态｜触发条件｜目标状态｜结果` 表格 | 业务实体生命周期及转换                                                     |
| `数据`     | 有数据契约时                 | 结构化无序列表                            | 接口、表、数据库函数、存储、事件和权限契约                                 |
| `运行`     | 有运行要求或声明制品时       | 固定命名分组                              | 当前卡片特有的配置、命令、迁移、回填、发布、回滚和可观测性要求             |
| `异常`     | 有失败场景时                 | 编号异常列表                              | 失败场景及处理策略                                                         |
| `文件`     | 必选                         | 固定分组及路径列表                        | 正式代码和测试代码的预定目录及完整文件路径                                 |
| `测试`     | 必选                         | 固定分组                                  | AC 与测试文件的对应关系、测试命令、覆盖率报告和阈值                        |
| `验收`     | 必选                         | AC 列表                                   | 可执行的 AC，以及提供物和异常的覆盖关系                                    |
| `实现记录` | 符合「记录」规定的出现条件时 | 状态对应的固定分组                        | 分类后的实现文件变更、测试和覆盖率结果、关键决策、修订原因及非阻塞遗留事项 |

卡片正文严格遵循表中结构。必选章节填写实际内容；条件章节在出现条件成立时填写实际内容，否则省略。内容超出固定结构的承载范围时，拆分卡片。

「文件」「测试」「实现记录」章节中的命名分组统一使用以下规范模板：

```markdown
<分组名称>：

- <列表项>
```

分组标签独占一行，不使用列表标记。标签与列表之间、相邻分组之间各有且仅有一个空行；每个出现的分组至少包含一个列表项。具体标签、顺序和出现条件由所属章节规定。

以下内容统称机器条目：

- 「提供」章节的列表项
- 「运行」章节中「运行命令」分组的列表项
- 「测试」章节中「测试文件」分组的路径列表项，以及「测试命令」和「覆盖率」分组的列表项
- 「文件」章节各分组的路径列表项
- 「实现记录」章节中各文件记录分组和「测试」分组的列表项

机器条目只包含对应章节规定的原始文本，固定分隔符使用相应格式指定的全角或半角符号。路径、命令、字段和代码标识出现在其他叙事内容中时，使用反引号标记。

状态在 YAML 中写为双引号字符串，在 README 正文的 `STATUS` 字段中写为不带标记的小写原始文本，在叙事内容中使用反引号。日期统一写为 `YYYY-MM-DD`。

各条件章节使用以下固定结构：

- 「术语」中的术语保持唯一
- 「规则」和「界面」的每个列表项表达一项要求
- 「交互」的每个列表项表达一个连续步骤
- 「状态」表格的四列固定为 `当前状态`、`触发条件`、`目标状态`、`结果`，每行表达一次转换
- 「数据」的第一层条目格式为 `- <对象类型>：<标识>`，对象类型集合为 `接口`、`表`、`函数`、`存储`、`事件`、`权限`；`接口`、`存储`、`事件`和`权限`的第二层条目格式为 `- <属性>：<值>`；`表`和`函数`使用下文规定的专用结构

「目标」「规则」「约束」「交互」「界面」「状态」「数据」「运行」和「异常」中的每项可验证要求，都必须由至少一条 AC 断言。

卡片正文统一遵守以下表达要求：

- 内容限定于当前交付需要实现和验证的事实，每项事实归入承担该职责的唯一卡片
- 正向说明应执行的动作、形成的状态和返回的结果
- 安全拒绝、数据保护和失败关闭等必要限制应明确写出触发条件、保持不变的事实和对外结果
- 背景和原因仅保留影响实现或验收的必要信息
- 语句保持流畅、清晰、简洁，同一术语、字段和动作使用一致名称

#### 数据

「数据」以对象为单位声明当前卡片的数据契约。对象依次按 `接口`、`表`、`函数`、`存储`、`事件`、`权限` 排列；同一类型的对象按领域依赖和阅读顺序稳定排列。

`接口`、`存储`、`事件`和`权限`使用两层无序列表。同一对象内的第二层属性保持唯一，并按稳定的语义顺序排列；属性值完整说明当前卡片需要约束的请求、响应、载荷、权限动作或存储行为。`表`和`函数`分别使用数据库章节规定的专用结构。

#### 数据库

数据库规范由通用规范和按数据库产品划分的子章节组成。当前只定义 PostgreSQL；增加其他数据库产品时，应新增与 PostgreSQL 同级的子章节，并保持通用规范的结构和语义不变。

表结构只在拥有该表的卡片中维护。当前卡片「数据」章节中的每个表都与「提供」章节中的表提供物一一对应。使用其他卡片拥有的表时，通过直接前置和外键目标引用其表提供物，不重复声明表结构。表对象必须遵守数据库通用规范，并使用规格上下文选定的一种具体数据库规范。同一交付中的表及其迁移使用同一数据库产品和具体规范版本；改变选择时，必须显式声明兼容与迁移方案。

##### 通用规范

数据库通用规范定义表设计、迁移和验证的共同要求，不绑定数据库产品。具体数据库规范将这些要求映射为对应产品的类型、默认值、约束、触发器、注释和 `schema` 核对方式。

表使用三层字段结构，通用模板如下：

```markdown
- 表：<表名>
  - COMMENT：<中文表用途>
  - 字段：
    - <字段名>：<具体数据库类型、空值与默认值声明>；COMMENT '<中文字段语义>'
  - 主键：<字段名或字段组合>
  - 外键：<本地字段> → <目标表>.<目标字段>
  - 唯一：<字段名、字段组合或约束表达式>
  - 检查：<检查表达式>
  - 索引：<索引字段、表达式、排序、访问方法与条件>
  - 触发器：<触发器名称、时机与行为>
  - 删除行为：<外键及其删除行为>
```

表结构遵守以下通用规则：

- 每张表恰有一项 `COMMENT` 和一个 `字段` 分组；表 `COMMENT` 使用非空中文说明领域用途，不包含秘密、凭据、个人信息样本或其他敏感数据
- `字段` 分组至少包含一项字段，第三层字段格式固定为 `- <字段名>：<具体数据库类型与列约束>；COMMENT '<中文注释>'`
- 字段名在同一张表内保持唯一，列表顺序定义目标物理列顺序。类型和列约束使用所选具体数据库的规范语法，明确类型、空值规则和适用的默认值。每个字段的 `COMMENT` 使用非空中文说明领域语义，并遵守表 `COMMENT` 的敏感数据边界
- 约定字段 `created_at` 由数据库在插入时自动设置；约定字段 `updated_at` 由数据库在插入及每次更新时自动设置；需要保留的业务发生时间使用其他语义字段
- 使用 UUID 的单列代理主键由数据库自动生成 UUID v7；外部标识、客户端幂等标识、外键和复合关系主键由业务写入，不套用该默认值
- `revision` 只用于多个调用方可能基于旧值覆盖同一组可编辑事实的场景。卡片必须明确其保护字段；更新这些字段时携带期望修订，并以主键和期望修订共同匹配。数据库将匹配行的修订值恰好递增一次；影响行数为 `0` 时返回修订冲突
- 明确目标值的幂等写入、单调状态转换、具有唯一执行权的任务、内部计数器和相互独立字段更新使用各自条件更新，不增加行级 `revision`；未改变 `revision` 保护字段的更新不推进该修订号
- 独立表承载具有独立生命周期、查询入口、关系基数或完整性约束的持久事实；严格一对一并且共同创建、访问和清理的实现状态合并到其所属事实
- 短期挑战、一次性票据、限流计数、活动令牌和加密 nonce 等自动到期且没有独立历史查询要求的运行状态，应使用支持 TTL 和所需原子操作的临时存储；需要长期审计的结果写入审计事实，不依赖保留临时状态实现
- 类型目录、强制属性、展示模式等可由稳定登记事实确定的属性在登记位置维护，业务行保存稳定键和实际取值，不逐行复制派生属性
- 表约束位于 `字段` 分组之后，并按 `主键`、`外键`、`唯一`、`检查`、`索引`、`触发器`、`删除行为` 的顺序排列。不适用的分组省略；`主键` 至多出现一次，其余分组标签可以重复，并按字段和依赖关系的稳定物理声明顺序排列。具体数据库要求命名的约束及其他 `schema` 对象使用稳定且在其命名作用域内唯一的名称
- `外键`、`唯一` 和 `索引` 的复合字段使用 `、` 连接，同一分组中的多项独立定义使用 `；` 分隔；索引条目明确影响实现的字段顺序、排序方向、表达式、访问方法和条件，未声明的部分使用具体数据库规范规定的默认值
- 主键和外键的本地字段必须已在当前表的 `字段` 分组中声明，外键目标字段必须由全树索引中的唯一表提供物声明。唯一、检查、索引和触发器中可确定提取的字段引用必须指向当前表字段；其余表达式、条件和数据库语义由 Card Review 与动态检查验证
- 每个触发器使用当前表内唯一且含表名的稳定名称；存在外键时明确每个外键的删除行为，不依赖数据库隐式默认值
- 本节列表是表结构在卡片内的权威表达，不得在同一卡片中使用 SQL 代码块重复维护相同结构。每张表至少有一个前向迁移落点，该落点必须位于「文件」声明的正式代码边界内；同一迁移可以原子建立或演进多张内聚表。AC 和测试命令必须覆盖迁移执行、重复执行和实际 `schema` 核对
- 迁移执行复用规格上下文提供的后端原生迁移入口。表卡只声明本卡片拥有的迁移定义，不为单表或业务模块另建脚本运行时迁移器，也不重复声明直接前置已经提供的迁移配置和命令。当前卡片新增或改变迁移配置、命令或其他运行条件时，按「运行」章节记录对应增量
- 规格上下文必须通过「运行环境版本」或适用的外部规范明确选择具体数据库规范；同一交付范围使用多种数据库时，每个表均能唯一确定所适用的具体数据库规范

数据库函数使用以下通用结构：

```markdown
- 函数：<函数名>
  - COMMENT：<中文函数用途>
  - 输入：<参数及其语义>
  - 输出：<返回值及其语义>
  - 事务：<原子性、锁和回滚行为>
  - 并发：<并发执行要求>
  - 权限：<执行身份与最小权限要求>
```

数据库函数遵守以下通用规则：

- 每个函数恰有一项中文 `COMMENT`，并明确当前实现所需的输入、输出和事务行为；并发或权限会影响外部结果时，增加对应属性
- 函数参数、返回值、事务、并发和权限只在函数对象中维护，规则与约束引用函数名称和已定义语义，不重复维护实现代码
- 每个函数至少有一个前向迁移落点，该落点必须位于「文件」声明的正式代码边界内。AC 和测试命令必须覆盖函数创建、重复迁移、实际定义核对，以及函数的正常、异常和并发行为

##### PostgreSQL

规格上下文选择 PostgreSQL 时使用 PostgreSQL 18 或更高版本，并要求迁移与运行环境提供内置 `uuidv7()`。PostgreSQL 表在数据库通用规范基础上使用以下具体模板：

```markdown
- 表：permissions
  - COMMENT：权限目录
  - 字段：
    - permission_id：UUID NOT NULL DEFAULT uuidv7()；COMMENT '权限标识'
    - permission_code：TEXT NOT NULL；COMMENT '权限码'
    - created_at：TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP；COMMENT '创建时间'
    - updated_at：TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP；COMMENT '更新时间'
  - 主键：permission_id
  - 唯一：permission_code
  - 触发器：permissions_set_updated_at BEFORE UPDATE，使用数据库 CURRENT_TIMESTAMP 覆盖 updated_at
```

PostgreSQL 表结构遵守以下具体规则：

- 字段类型、空值规则和默认值使用 PostgreSQL SQL 语法。字段格式中的 `COMMENT` 是结构规格的规范简写；迁移使用 `COMMENT ON TABLE` 和 `COMMENT ON COLUMN` 将表和字段的中文注释写入 PostgreSQL catalog
- PostgreSQL 前向迁移定义使用 PostgreSQL 原生 DDL 语义，并由规格上下文提供的后端原生迁移入口统一发现、排序、记录和执行
- 表、字段、约束、索引、触发器和函数标识符使用不加引号的小写 `snake_case`，避开 PostgreSQL 保留关键字，并在 PostgreSQL 对应命名作用域内保持唯一
- PostgreSQL 索引未声明访问方法时使用 B-tree，字段未声明排序方向时使用 `ASC`；迁移为索引设置稳定名称，主键或唯一约束已经提供等价索引时不重复创建普通索引
- 约定字段 `created_at` 固定声明为 `TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP`
- 约定字段 `updated_at` 固定声明为 `TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP`，并由当前表独立命名的 `BEFORE UPDATE` 触发器在每次更新时以 `CURRENT_TIMESTAMP` 覆盖
- 使用 UUID 的单列代理主键固定声明为 `UUID NOT NULL DEFAULT uuidv7()`
- 约定并发控制字段 `revision` 固定声明为 `BIGINT NOT NULL DEFAULT 0`。应用更新受保护字段时，以主键和 `revision = <期望值>` 共同匹配，且不直接写入 `revision`；当前表独立命名的 `BEFORE UPDATE` 触发器仅在声明的保护字段发生变化时，将匹配行的 `NEW.revision` 设为 `OLD.revision + 1`
- 外键删除行为使用显式 `ON DELETE` 子句；主键、外键、唯一、检查、索引和触发器使用 PostgreSQL 支持的语法，并由迁移测试通过 PostgreSQL catalog 核对实际结构

#### 约束

「约束」承载可验证的硬指标和明确的技术边界。各分组按「代码目录树」「技术栈版本」「运行环境版本」「约束要求」的顺序排列：

- 声明正式代码目录时，「代码目录树」必选，标签固定为 `代码目录树：`，正文为一个 `text` 代码块
- 存在技术栈版本要求时使用「技术栈版本」分组，标签固定为 `技术栈版本：`，表格列固定为 `技术` 和 `版本`
- 存在运行环境版本要求时使用「运行环境版本」分组，标签固定为 `运行环境版本：`，表格列固定为 `环境` 和 `版本`
- 版本下限在单元格中写为 `>= 1.3.0`，精确版本写为 `1.3.0`；“可用”等非版本条件写入「约束要求」
- 存在其他约束时使用「约束要求」分组，标签固定为 `约束要求：`，正文为一层无序列表，每项表达一项要求
- 没有正式代码目录时省略「代码目录树」；没有对应内容时省略其他可选分组；整个章节至少包含一个分组
- 目录树以正式代码目录或多个正式代码路径的共同祖先为根，列出本卡片建立或约束的稳定目录、关键入口、配置、生成文件，以及该范围内的测试落点；其他测试落点由「文件」章节中的测试代码边界声明
- 尚未定义的业务模块以扩展目录表示，定义业务文件时再写入目录树
- 目录树中的路径落在「文件」章节声明的范围内，并保持既定写入边界
- AC 检查目录落点与代码目录树的一致性

「约束」使用以下规范模板，并按当前内容省略不满足出现条件的分组：

````markdown
代码目录树：

```text
src/user/register/
├── index.ts
└── tests/
    └── register.test.ts
```

技术栈版本：

| 技术 | 版本       |
| ---- | ---------- |
| Bun  | `>= 1.3.0` |

运行环境版本：

| 环境       | 版本      |
| ---------- | --------- |
| PostgreSQL | `>= 18.0` |

约束要求：

- 生产日志只保留运行所需的非敏感信息
````

#### 运行

「运行」只记录当前卡片相对于项目既有运行条件新增或改变的要求。各分组按「运行配置」「运行命令」「运行要求」的顺序排列：

- 存在运行配置时使用「运行配置」分组，标签固定为 `运行配置：`，表格列固定为 `配置` 和 `要求`，每项配置恰占一行
- 存在供使用者直接执行的运行命令时使用「运行命令」分组，标签固定为 `运行命令：`，正文为一层命令列表，每项命令使用仓库根目录作为工作目录
- 存在配置和命令以外的运行要求时使用「运行要求」分组，标签固定为 `运行要求：`，正文为一层无序列表，每项表达一项要求
- 没有对应内容时省略相应分组；整个章节至少包含一个分组

「运行」使用以下规范模板，并按当前内容省略不满足出现条件的分组：

```markdown
运行配置：

| 配置           | 要求                 |
| -------------- | -------------------- |
| `DATABASE_URL` | 必填，数据库连接地址 |

运行命令：

- bun run user:serve

运行要求：

- 发布制品使用语义化版本
```

### 提供

「提供」章节每行只写一个规范化标识：

```markdown
- 接口：POST /api/register
- 页面：/register
- 表：users
- 组件：RegisterForm
- 命令：user:seed
- 存储：Valkey 注册验证码
- 事件：UserRegistered
- 制品：user-service-image
- 能力：手机号注册
```

- 类型集合依次为 `接口`、`页面`、`表`、`组件`、`命令`、`存储`、`事件`、`制品`、`能力`；条目按该类型顺序排列，同一类型按完整提供物字典序排列
- 接口值为“大写方法 + 路径”，页面值为路由，表值不带“表”字；组件、命令、存储、事件和能力使用非空、稳定且含义明确的项目内标识
- 制品值使用稳定的项目内标识，适用于 OCI 镜像、静态包、客户端安装包和库包。卡片声明制品时，「运行」成为必选章节，并写明实际位置和版本规则
- 使用全角冒号，一行一个提供物；清理首尾空白后的整行就是唯一键
- 同一提供物在一张卡片内只出现一次，并且在全库中只由一张卡片声明和负责

### 验收

「异常」格式为 `- EX-两位编号：场景与策略`。编号从 `EX-01` 连续递增且不重复；超过 `EX-99` 表示卡片过大，必须拆分卡片。

「验收」至少有一条 AC：

- 首行格式为 `- [ ] AC-两位编号 场景名` 或 `- [x] AC-两位编号 场景名`
- AC 从 `AC-01` 连续递增且不重复；超过 `AC-99` 必须拆分卡片
- 第一项固定为 `Covers`；关键字独占一行，下面写一个或多个非空列表项，每项引用当前卡片的完整提供物或 `异常：EX-nn`
- `Covers` 后依次写可选的 `Given`、恰好一个 `When` 和恰好一个 `Then`；`When` 使用一个非空行内列表项描述触发动作
- `Given` 和 `Then` 的关键字分别独占一行，下面写一个或多个非空列表项
- 没有 `Background` 时，每条 AC 都要写一个 `Given`；存在 `Background` 时，每条 AC 可以增加自身的 `Given`，并与公共前置条件共同构成该场景的前置条件
- `### Background` 至多出现一次，位于第一条 AC 前，正文为一个供全部 AC 共用的 `Given` 列表
- 参数化场景最后依次写 `Examples` 和缩进表格，占位符格式为 `<参数名>`，表头与参数名一一对应，数据行至少一行

`Background` 和 `Examples` 使用以下规范模板：

```markdown
### Background

- Given
  - <全部 AC 共用的前置条件>

- [ ] AC-01 参数化场景
  - Covers
    - <当前卡片的完整提供物或异常>
  - When 使用 <参数>
  - Then
    - 得到与 <参数> 对应的结果
  - Examples

    | 参数 |
    | ---- |
    | 值   |
```

每个提供物和异常至少被一条 AC 覆盖。Card Review 还要确认 AC 对相应契约形成有效断言；仅列出名称不构成有效覆盖。

每条 AC 由「测试」章节中对应的测试文件共同验证。每个测试文件可以包含一个或多个自动化验收场景，`Examples` 的每一行对应一个场景实例。

`draft` 和 `ready` 卡片的 AC 全部为 `[ ]`。`doing` 卡片的一条 AC 仅在其对应的全部测试文件及场景实例的当前结果有效且通过时为 `[x]`，任一结果失效或失败时为 `[ ]`；`done` 卡片的 AC 全部为 `[x]`。

### 文件

#### 声明

「文件」章节由「正式代码」和「测试代码」两个分组组成。每个出现的分组都使用固定标签，空一行后写路径列表：

```markdown
正式代码：

- src/routes/index.ts
- src/user/register/

测试代码：

- src/user/register/tests/
```

- 分组标签固定为 `正式代码：`、`测试代码：`
- 「正式代码」分组声明仓库根目录下的相对目录或完整文件路径；目录以 `/` 结尾，文件不以 `/` 结尾
- 「测试代码」分组声明仓库根目录下的相对目录或完整测试文件路径；目录以 `/` 结尾，文件不以 `/` 结尾
- 「测试代码」分组必选，并按技术栈和标准测试源集逐项声明
- 存在正式代码时，「正式代码」分组必选，可以包含一个或多个目录和完整文件路径
- 「测试代码」可以包含一个或多个目录和完整测试文件路径；使用完整测试文件路径时，每个路径恰好属于当前卡片
- 当前交付跨多个代码目录时，这些目录必须共同实现同一项内聚功能；能够独立提供、实现和验收的目录应拆分到不同卡片，并通过提供物和直接依赖连接
- 分组顺序固定为「正式代码」、「测试代码」；同一分组有多个路径时按字典序排列
- 「文件」章节按当前交付预计发生变更的文件类型声明全部路径，每项实际变更发生时均落在当时声明的范围内

#### 边界

- 「正式代码」和「测试代码」声明的目录及完整文件路径共同构成当前卡片的文件边界；其中的目录条目统称输出目录
- 项目结构允许时，正式代码目录和测试代码目录位于同一个卡片专属根目录下
- 每个输出目录只由全库一张卡片声明。项目结构需要时可以声明父子目录：更具体输出目录内的文件只属于该目录的范围，父级输出目录的范围自动排除这些文件
- 为新卡片分配更具体的输出目录时，该目录应尚不存在，或尚未包含其他卡片范围内的文件。如果已经包含，先完成相关卡片的文件边界修订、文件迁移和依赖影响处理，再由新卡片声明该目录
- 同一卡片的正式代码目录与各测试代码目录必须不同；测试代码目录可以相互独立，也可以放在正式代码目录内
- 测试代码目录位于正式代码目录内时，这些子目录仍只属于测试代码，并从正式代码范围和覆盖率分母中排除
- 「正式代码」中的完整文件路径可以位于输出目录内或目录外，也可以出现在多张卡片中；各卡片分别声明自己的预计修改范围
- 正式代码文件由创建它的卡片拥有，其后的归属保持不变。其他卡片在「正式代码」声明同一完整文件路径只表示预计修改该文件，不改变归属
- 「正式代码」中的每个完整文件路径直接指向一个正式代码文件，并位于测试代码目录之外；「测试代码」中的每个完整文件路径直接指向一个测试代码文件
- 路径使用仓库根目录下不含 `.` 或 `..` 片段的规范化相对路径，解析结果和符号链接目标均位于仓库内

#### 变更

卡片产生的文件变更按用途分为三类：

| 类别         | 范围                                                                       | 归属                                                                     |
| ------------ | -------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| 实现文件变更 | 当前卡片为完成交付而产生，且不属于规格文件变更的文件创建、修改、删除和移动 | 发生时的写入范围限定在文件边界内，事实累计写入「实现记录」的对应文件分组 |
| 规格文件变更 | 当前卡片和按 README 状态同步规则修改的 README                              | 不属于文件边界，不写入「实现记录」的文件记录分组                         |
| 临时输出     | 测试生成、由仓库忽略规则排除且不进入交付的构建文件、缓存文件和验收产物     | 位于仓库约定的构建目录、缓存目录或 `coverage/<卡片ID>/`                  |

当前卡片修改其他卡片拥有的正式代码文件时，只增加聚合内容，例如模块声明、导出、注册和装配调用。需要新增实质逻辑时，在当前卡片的输出目录内创建文件承载该逻辑，再由被修改文件按聚合方式引用。

聚合内容集中在被多张卡片共同修改的文件中，这些文件因此只保留可由结构直接读出的声明，不承载分支、计算或状态。

卡片只有一份覆盖率报告时，报告直接写入 `coverage/<卡片ID>/`；有多份覆盖率报告时，每份报告写入 `coverage/<卡片ID>/<技术栈>/`。覆盖率处理产生的中间文件可以写入 `coverage/<卡片ID>/`。其他测试报告、运行时截图和跟踪文件写入 `coverage/<卡片ID>/<工具或用途>/`；子目录名称只由小写英文字母、数字和单个短横线组成。测试命令或项目内测试配置必须明确指定生成路径。

`coverage/<卡片ID>/` 只保留验收所需的最小信息，并符合卡片声明的数据与权限边界。仓库忽略规则排除整个 `coverage/` 目录，其中的临时输出不纳入交付，也不写入「文件」章节或「实现记录」的文件记录分组。

临时输出不要求在卡片完成后保留；复验所需的临时输出由测试命令重新生成。

预期快照、视觉基准和固定夹具属于测试代码，保存在当前卡片的测试代码边界内，并写入「实现记录」。

### 测试

#### 格式

「测试」章节按以下规范模板依次写「测试文件」、「测试命令」和「覆盖率」：

```markdown
测试文件：

- AC-01
  - src/user/register/tests/register-success.test.ts
- AC-02
  - src/user/register/tests/reject-duplicate.test.ts
- AC-03
  - src/user/register/tests/reject-invalid-credentials.test.ts

测试命令：

- bun test src/user/register/tests --coverage --coverage-reporter=lcov --coverage-dir=coverage/USER-01
- bunx playwright test src/user/register/tests --output=coverage/USER-01/playwright

覆盖率：

- 报告：javascript-typescript；lcov；coverage/USER-01/lcov.info
- 行覆盖率：>= 90%
- 函数覆盖率：>= 80%
```

#### 文件

- `测试文件` 按 AC 编号升序列出当前卡片的全部 AC，第一层列表项固定为 `- AC-nn`，每项下面写一个或多个缩进的测试文件路径；同一 AC 下的路径无重复并按字典序排列
- 一条 AC 可以对应多个测试文件，同一测试文件也可以覆盖当前卡片的多条 AC。同一测试文件在全树索引中只归属一张卡片，并包含能够明确对应各项已声明 AC 的可执行测试用例
- 每个路径指向当前卡片「文件」章节测试代码边界内的一个具体文件，使用规范的仓库相对路径，不使用目录、通配符、`.` 或 `..`；解析结果和符号链接目标均位于对应的测试代码目录内或等于声明的完整测试文件路径
- 每个测试文件在卡片完成时必须存在并包含至少一个可执行验收测试用例；每条 AC 由所列文件中明确对应它的用例共同形成验收证据
- 共享夹具、测试工具，以及只验证内部单元而不直接验证完整 AC 的测试文件，不写入「测试文件」，也不用于判定 AC 覆盖
- 测试文件名不要求包含 AC 编号；「测试文件」是 AC 与测试文件对应关系的唯一权威位置，文件移动、重命名、拆分或合并时同步更新

#### 命令

- `测试命令` 列表至少包含一项命令，且各项命令互不相同。每项命令使用仓库声明并锁定的工具、包装器、包管理器、环境管理器或项目脚本完成准备、测试或验证
- 命令按列表顺序从仓库根目录执行，整组命令必须非交互、可重复，并自行准备和清理测试状态
- 每项命令使用退出状态表达结果，`0` 表示通过，非零值表示失败
- 敏感配置在命令中引用配置项名称并由批准的安全设施注入；非敏感测试值可以直接写入
- 一条测试命令可以验证多条 AC；「测试文件」列出的每个文件及其全部场景实例必须由至少一条命令实际收集和执行

#### 覆盖率

有可计量代码时，`覆盖率` 列表依次包含一项或多项 `报告`、一项 `行覆盖率` 和一项 `函数覆盖率`；没有可计量代码时，只包含一项 `覆盖率不适用：<具体原因>`。

阈值条目的固定格式为 `- 行覆盖率：>= <百分比>%` 和 `- 函数覆盖率：>= <百分比>%`。百分比使用 `0` 至 `100` 的十进制数，最多保留两位小数。

覆盖率报告条目的固定格式为 `- 报告：<技术栈>；<格式>；<路径>`。技术栈使用匹配 `[a-z][a-z0-9-]*` 的仓库内稳定标识，报告按技术栈标识字典序排列。报告格式按以下顺序确定：

1. 仓库对当前技术栈已有唯一的 `lcov` 或 `jacoco-xml` 格式约定时沿用该格式
2. 仓库没有格式约定且项目既有测试入口只能生成 JaCoCo XML 时使用 `jacoco-xml`
3. 其他情况使用 `lcov`

格式集合为 `lcov` 和 `jacoco-xml`。路径由报告数量和格式唯一确定：

| 格式         | 单报告路径                     | 多报告路径                              |
| ------------ | ------------------------------ | --------------------------------------- |
| `lcov`       | `coverage/<卡片ID>/lcov.info`  | `coverage/<卡片ID>/<技术栈>/lcov.info`  |
| `jacoco-xml` | `coverage/<卡片ID>/jacoco.xml` | `coverage/<卡片ID>/<技术栈>/jacoco.xml` |

覆盖率针对当前卡片拥有的全部可计量正式代码，即「实现记录」中「创建文件」分组内的正式代码文件。覆盖率命令的排除表达式由该分组机械推导，不单独维护；「创建文件」增减时同步更新该表达式。

- 每份覆盖率报告完整包含对应技术栈的全部应计量文件；LCOV 的函数计数和 JaCoCo XML 的方法计数用于函数覆盖率，完整包含应计量文件是达到覆盖率阈值的前提
- 行覆盖率和函数覆盖率分别按“覆盖数 ÷ 总数 × 100%”聚合计算；总数为零时，对应覆盖率记为 `100.00%`
- 阈值使用未舍入的比率判断，实际值统一保留两位小数写入「实现记录」

默认行覆盖率为 `>= 90%`、函数覆盖率为 `>= 80%`，卡片声明的阈值适用于每份覆盖率报告。提高默认值时直接声明新阈值；降低默认值时必须在「约束」逐项写明原因并由人明确批准。

生成代码和第三方代码等覆盖率排除项在「约束」中逐项声明。当前卡片无可计量正式代码时，「覆盖率」列表固定写为 `- 覆盖率不适用：<具体原因>`，并保留验证 AC 的命令。

覆盖率检查按照报告条目逐份读取当前卡片的 LCOV 或 JaCoCo XML，重新计算行覆盖率和函数覆盖率，并确认每份报告都达到阈值。代码覆盖率衡量执行情况，AC 用于独立验证行为。

#### 并行

并行测试的构建目录、缓存目录、`coverage/<卡片ID>/`、数据库或 `schema`、队列和其他可变资源按卡片 ID 隔离。无法隔离的可变测试资源不得并行使用。

### 依赖

卡片 `depends` 由同一主题目录中的直接前置卡片 ID 组成：

- 有依赖时使用按 ID 字典序排列的非空 YAML 块状列表
- 无依赖时省略 `depends`
- 依赖目标唯一且不指向当前卡片，卡片依赖图保持有向无环
- 通过另一张前置卡片间接获得的能力由递归遍历得出，`depends` 保留最小直接边集合
- 当前卡片自身也直接使用该能力时，可以同时声明相应直接边
- 列表顺序不表示优先级，每次增删或改向后必须重算完整卡片依赖图

卡片依赖方向恒定为“当前卡片 → 直接前置卡片”。直接前置位于其他主题目录时，按 README 的主题依赖规则将该关系提升为主题依赖。第三方库、外部服务和环境变量写入卡片中对应的规格章节。

增删或改向卡片的 `depends` 属于卡片修订。当前卡片必须转换为 `draft`，重新执行 Card Lint 和 Card Review，并再次经过规格确认后，才能重新成为 `ready`。

### 记录

「实现记录」中的文件记录累计保存当前卡片已经产生的实施事实，其他分组保存当前卡片轮次内的有效事实。正确的文件记录不因后续修改、状态转换或卡片修订而被删除、覆盖或重新分类；只有删除卡片时，才随卡片一并删除。经可复核证据确认有误的条目不属于实施事实，应直接修正其分组或路径，或者移除该条目，不保留更正历史。

#### 完成

卡片完成时的「实现记录」章节使用以下规范模板：

```markdown
创建文件：

- src/user/register/index.ts
- src/user/register/normalize.ts
- src/user/register/tests/register-success.test.ts
- src/user/register/tests/reject-duplicate.test.ts
- src/user/register/tests/reject-invalid-credentials.test.ts

修改文件：

- src/routes/index.ts

测试：

- bun test src/user/register/tests --coverage --coverage-reporter=lcov --coverage-dir=coverage/USER-01；通过
- 覆盖率（javascript-typescript）：行覆盖率 94.20%；函数覆盖率 87.50%

决策：

- 注册处理器沿用仓库现有的 `service` 与 `repository` 分层

遗留：

- 旧用户手机号格式清理不属于当前卡片，且不影响当前 AC
```

- 完成记录的分组顺序固定为「创建文件」、「修改文件」、「删除文件」、「移动文件」、「测试」、「决策」、「遗留」；当前卡片尚未产生对应文件事实时省略相应文件分组
- 当前卡片产生过实现文件变更时至少出现一个文件分组；「测试」分组必填，有关键决策时写「决策」分组，有非阻塞遗留事项时写「遗留」分组
- 当前卡片首次创建一个实现文件时，将当时的完整路径写入「创建文件」。当前卡片后续修改该文件时，该文件仍归入「创建文件」，不写入「修改文件」
- 当前卡片修改一个由既有工程或其他卡片创建的实现文件时，将完整路径写入「修改文件」
- 当前卡片删除或移动实现文件时，分别追加「删除文件」或「移动文件」记录，并保留已有的「创建文件」和「修改文件」记录
- 「创建文件」「修改文件」「删除文件」的每个列表项是一条完整的仓库相对文件路径。「移动文件」的每个列表项固定为 `<完整源文件路径> -> <完整目标文件路径>`
- 正式实现文件创建后又删除时，保留「创建文件」并追加「删除文件」；构建文件、缓存文件和验收产物按临时输出规则处理，不写入文件记录
- 文件记录经可复核证据确认有误时，直接修正为正确的分组或路径；记录的实施事实不存在时，直接移除错误条目。纠错不新增文件记录分组，也不保留历史条目
- 同一完整列表项在「创建文件」、「修改文件」、「删除文件」或「移动文件」的同一分组中只出现一次，这些分组分别按完整列表项字典序排列；一条路径因先后发生不同事实可以出现在多个分组中
- 每项实现文件变更都必须在当时有效的文件边界内发生。后续文件边界变化不改变或删除已记录的实施事实；不同卡片可以记录同一完整文件路径
- 「测试」分组先按「测试」章节的顺序记录每条完整命令并追加 `；通过`，命令文本与「测试」章节完全一致
- 全部命令记录之后，按技术栈标识字典序记录每项覆盖率结果，格式为 `覆盖率（<技术栈>）：行覆盖率 <实际值>；函数覆盖率 <实际值>`；覆盖率不适用时照抄 `覆盖率不适用：<具体原因>`
- 「决策」分组只记录影响后续维护的关键实现决策
- 「遗留」分组限于不影响卡片规格和 AC 的已知限制；未完成的必需工作会阻止完成
- 每个出现的分组至少包含一个记录项

#### 实施

卡片进入 `doing` 后，按以下规则维护「实现记录」：

- 每次产生实现文件变更时，按创建、修改、删除或移动的事实追加尚未记录的列表项；已有文件记录保持不变
- 文件记录经可复核证据确认错误时，按完成记录规则直接修正或移除错误条目
- 形成影响后续维护的关键实现决策时增加「决策」分组；没有关键决策时省略
- 全部测试命令和覆盖率检查通过时增加「测试」分组；任一记录失效时删除该分组
- `doing` 卡片不包含「遗留」分组

#### 修订

卡片因修订从 `ready`、`doing` 或 `done` 转换为 `draft` 时，应将实现记录更新为当前事实，并使用「修订」分组记录原因：

```markdown
修订：

- <事实、证据和修订原因>
```

修订后的「实现记录」按以下规则更新：

- 修订只改变规格和验证状态，全部正确的文件记录保持不变，不得删除、覆盖或重新分类；修订本身不构成文件记录纠错依据
- 已经存在文件记录时，在这些分组之后写「修订」分组；尚未产生文件记录时只写「修订」分组
- 修订时删除「测试」、「决策」和「遗留」分组
- 修订后的实施过程再次产生文件事实时，按实施规则追加尚未记录的列表项，已有文件记录仍保持不变
- 问题解决后删除「修订」分组并保留全部文件记录；没有其他分组时删除整个「实现记录」章节
- 卡片再次完成时保留并补齐累计文件记录，再按完成记录格式写入当前有效的其他分组

### 检查

#### Card Lint

至少检查以下确定性规则：

- YAML 字段集合与顺序、ID、主题目录名、卡片的文件名、一级标题、状态、日期和 `depends`
- 必选章节、条件章节、固定顺序、各章节固定结构、重复标题、空章节和 `无` 占位
- 「数据」对象类型顺序正确；表数据对象与当前卡片的表提供物一一对应，同一表只由全库一张卡片维护结构；数据库函数对象的名称、中文注释、输入、输出、事务、并发和权限属性符合出现条件
- 每张数据库表具有唯一的中文 `COMMENT` 和唯一的 `字段` 分组；字段名、字段格式、字段的中文 `COMMENT`、目标物理顺序、分组出现次数、约束顺序、外键箭头、复合字段与多项定义分隔符，以及可确定提取的 `schema` 对象名称均有效；所选具体数据库规范能够依据规格上下文唯一确定
- 每张数据库表的约定时间字段、UUID v7 单列代理主键和 `revision` 字段符合通用声明要求；主键、外键及其他可确定提取的约束引用有效，触发器名称符合稳定命名要求
- 选择 PostgreSQL 时，标识符、字段类型、默认值、注释、时间字段、UUID v7 单列代理主键、`revision` 字段、约束和触发器声明符合 PostgreSQL 数据库规范
- 每张数据库表具有位于正式代码边界内的前向迁移落点，并由至少一条 AC 及其测试文件覆盖迁移执行、重复执行和实际 `schema` 核对
- 表卡没有为单表或业务模块新增脚本运行时迁移器，也没有重复声明规格上下文已经提供的迁移配置和命令；迁移定义位于后端原生迁移入口能够发现的正式代码边界
- 提供物格式与唯一性，异常和 AC 的连续编号
- `Covers`、`Given`、`When`、`Then`、`Background` 和 `Examples` 结构
- 每个提供物和异常都有覆盖，AC 勾选与状态一致
- 「测试文件」的分组位置、AC 编号与顺序、路径排序和路径格式正确，每条 AC 对应至少一个测试文件
- 每个测试文件在全树索引中只归属一张卡片，可以在该卡片的多条 AC 下出现；文件按最具体输出目录或声明的完整测试文件路径归属当前卡片，路径解析结果和符号链接目标均位于测试代码边界内
- 测试代码边界必填；正式代码分组和代码目录树的出现条件成立；代码目录树与文件范围一致
- 不同卡片的输出目录保持唯一，父子目录按最具体目录确定范围，已有文件的边界调整符合迁移规则
- 「文件」章节的分组标签、出现条件、顺序和路径列表格式正确；同一卡片的多个目录共同实现一项内聚功能，完整文件路径使用规范格式，正式代码完整文件路径允许在多张卡片中出现
- 临时输出位于仓库约定的构建目录、缓存目录或 `coverage/<卡片ID>/`，需要并行隔离的路径按卡片 ID 隔离
- 测试命令的工具来源、顺序、退出状态和文本格式，覆盖率报告与可计量技术栈一一对应，报告格式、路径、阈值、降级和不适用原因有效
- 「提供」「文件」「测试」章节和「实现记录」章节中的机器条目使用原始文本
- 实现记录的出现条件、文件事实分类、分组顺序、原因分组、分组内唯一性和排序、机器条目格式及当前状态一致

#### 动态检查

当前卡片完成前还要执行全部测试命令和覆盖率检查，并完成以下检查：

- 当前实施产生的每项实现文件事实都已按创建、修改、删除或移动语义写入「实现记录」，新增事实发生时均符合当时有效的文件边界
- 正确的已有文件记录未被删除、覆盖或重新分类，全部文件记录都与可复核的实施事实一致
- 规格文件变更符合状态同步规则，全部临时输出位于仓库约定的构建目录、缓存目录或 `coverage/<卡片ID>/`
- 当前实施实际变更的重叠文件已按最新集成基线计算完整影响范围，并完成所需的影响验证
- 每个测试命令都有唯一的通过记录。「测试文件」列出的每个文件都存在，并包含至少一个可执行验收测试用例；其中的全部测试用例以及每个 `Examples` 数据行对应的场景实例都已被实际收集、执行并通过
- 声明数据库表时，通过规格上下文提供的后端原生迁移入口，从每个适用且受支持的起始 `schema` 执行前向迁移，并重复执行迁移命令。迁移必须成功，重复执行后 `schema` 与既有数据保持不变
- 数据库表的实际物理列顺序、类型、空值规则、默认值、约束、索引、触发器、删除行为以及表和字段注释，均依据所选具体数据库规范与「数据」章节核对一致
- 声明数据库函数时，通过规格上下文提供的后端原生迁移入口执行并重复执行前向迁移。迁移必须成功；重复执行后，函数定义与既有数据保持不变；函数名称、参数、返回值、事务、并发和权限行为均与「数据」章节一致
- 存在相应约定字段时，运行测试验证以下行为：UUID 单列代理主键由数据库生成 UUID v7；`created_at` 与 `updated_at` 按声明自动设置；携带期望 `revision` 更新受保护字段时，修订号成功递增一次；使用陈旧修订更新时，影响行数为 `0` 并返回修订冲突；未改变受保护字段的更新不改变修订号
- 选择 PostgreSQL 时，迁移测试通过 PostgreSQL catalog 核对实际结构，并验证 PostgreSQL 数据库规范规定的默认值、注释和触发器行为
- 「实现记录」中的测试和覆盖率结果与实际输出一致

#### Card Review

至少判断以下需要规格语义判断的内容：

- 当前卡片及其 README 链是否完整承载外部规范规定的交付行为和边界
- 当前卡片内容是否限定于当前需要实现和验收的功能，是否使用正向、流畅、清晰且简洁的语句表达行为与结果，是否只保留构成实现契约的拒绝、失败和禁止边界
- 当前卡片是否自洽，声明的交付上下文是否足以直接实现一致的外部行为
- 每张表、每个数据库函数以及每项临时存储、缓存、异步流程、状态和修订号是否具有明确的当前消费者与独立职责，是否能够由已有持久事实、条件更新、同步调用或稳定登记直接表达
- 父 README 中的卡片功能描述是否准确概括当前卡片的提供物及作用
- 当前卡片是否与兄弟卡片、直属子主题目录职责正交
- 每条依赖是否确为直接前置，方向和依赖层级是否正确
- 文件边界是否足够、最小且便于后续卡片扩展，「正式代码」中的完整文件路径是否属于当前卡片的预计修改范围
- 所有可验证要求是否进入 AC，测试文件及其全部用例是否确实归属并验证对应 AC，测试命令是否真正执行这些文件
- 删除、前置修订或重叠文件变更的影响验证范围是否完整，相关契约是否兼容
- 文件记录是否完整表达当前卡片累计产生的文件事实，同一卡片创建的文件在后续修改时是否仍保持创建语义，修订时是否保留全部正确的已有文件记录
- 数值、文案、权限、边界、迁移、回滚、兼容和可观测性要求是否可断言
- 数据库表的唯一归属、数据库函数的职责、字段与参数语义、注释的数据边界、约束表达式、外键目标与删除行为、事务与并发行为、迁移顺序和兼容策略是否完整、正确且不存在重复维护；迁移入口是否保持唯一并由规格上下文拥有；具体实现是否符合规格上下文选择的数据库规范
- 技术选择是否依次遵循当前交付的外部规范、当前卡片、必要的直接前置卡片、仓库配置和现有工程约定

### 完成

编码 AI 在当前卡片的最新工作树上执行完成门禁，并以实际文档、代码、命令输出和运行结果逐项形成可复核结论。

完成门禁依次确认以下条件：

- 全部测试命令和覆盖率检查通过
- 实际变更的重叠文件已完成影响验证
- Card Lint 和动态检查全部通过
- 必需工作全部完成
- 文档、代码、测试和配置与提供物、文件边界、AC 及「实现记录」一致
- 同一最新工作树连续通过三轮独立审核，每轮的问题清单均为空

每轮独立审核重新执行 Card Review、规范符合性检查、影响范围判断和全部完成事实核对，并确认 Card Lint、动态检查、测试命令、覆盖率检查及影响验证的证据适用于当前工作树。每轮单独形成审核结论和问题清单；全部审核判据成立且问题清单为空时，累计一次连续通过。

每轮发现的问题都在进入下一轮前完成修正。修正后的最新工作树进入新的审核周期，连续通过次数从第一轮开始重新累计；修正影响已有验证证据时，重新执行对应测试或检查，以形成适用于最新工作树的有效证据。

上述条件全部成立后，完成门禁通过。随后将卡片从 `doing` 转换为 `done`，并按照 README 状态同步规则更新父 README 投影和全部祖先聚合状态。

规格、依赖、提供物或文件边界问题触发卡片按修订规则转换为 `draft`；其他待完成条件使卡片保持 `doing`。前置修订的影响验证在修订卡片完成后进行，当前卡片的完成门禁按上述条件独立判定。

### 提交

卡片完成后，建议创建与该卡片对应的 Git 提交，以便追踪规格、实现和验证结果。提交方式和集成策略由当前交付的外部规范确定。
